# RAG 项目分阶段重构方案

## 总目标

把当前已能本地运行的 RAG 项目，重构成一个面试作品级的设备运维 RAG Agent。重点学习 RAGFlow 的优点，但不照搬完整平台：补齐文档状态闭环、主题/别名体系、chunk 可观测与启停、多路召回融合、引用可追溯、评测指标，并在稳定基线之上验证基于 GRPO 的 Query Planner 策略优化是否有真实收益。

## 阶段 0：项目基线稳定

目标：先保证后续重构有可靠回归入口。

- 统一测试入口为 `uv run python -m pytest`。
- 清理 import 阶段会真实执行图、模型、Milvus 的测试。
- 新增导入图、查询图 compile smoke test。
- 固化当前可运行流程：PDF/Markdown 导入、Milvus 入库、查询回答。
- 梳理现有配置项，避免服务启动时因为 Mongo/Milvus/模型懒加载失败而直接中断。

验收标准：

- pytest 可稳定运行。
- 导入图和查询图能独立 compile。
- 不连接外部模型时，基础单元测试仍可执行。

## 阶段 1：统一状态契约与分层边界

目标：让 LangGraph 节点、service、infra 的职责清晰，减少状态字段混乱。

- 整理 `ImportGraphState` 和 `QueryGraphState`。
- 约定节点统一返回 partial state，service 不随意写未知字段。
- 把 `item_name` 逐步替换为更通用的 `subject` 概念。
- 保留现有 `api / process / rag / infra / shared` 分层，但明确职责：
  - `api`：HTTP 入参出参。
  - `process`：LangGraph 编排。
  - `rag`：导入、检索、生成业务逻辑。
  - `infra`：Mongo、Milvus、MinIO、LLM 网关。
  - `shared`：通用工具、配置、日志。

验收标准：

- 导入和查询 state 字段有清晰注释。
- 节点不再混合过多业务细节。
- 旧流程行为保持不变。

## 阶段 2：主题体系重构

目标：把原来的 `item_name` 升级成更适合设备运维场景的知识体系。

- 新增标准主题表：一条标准主题一条数据，用于管理知识体系。
- 新增别名索引表：一个别名一条数据，用于主体识别和向量召回。
- chunk 关联标准主题，而不是只依赖文件名或 item_name。
- 支持轻量领域字段：
  - `equipment_model`
  - `alarm_code`
  - `part_name`
  - `sop_type`
  - `safety_level`
  - `maintenance_stage`

Milvus 设计：

- 标准主题 collection：管理标准主题向量。
- 别名 collection：管理别名向量，提升用户问题主体识别。
- chunk collection：管理最终内容检索。
- BGE-M3 在单个 Python 进程内使用线程安全的懒加载单例：首次构造和实际编码分别加锁，避免多文档并发导入时重复加载或同时操作同一模型。该保护不跨 Uvicorn worker，也不等于把整个导入流程串行化。

验收标准：

- 用户输入别名时能识别到标准主题。
- 查询最终按标准主题过滤 chunk。
- 旧的 item_name 逻辑可平滑迁移。

## 阶段 3：文档与任务元数据闭环

目标：学习 RAGFlow 的 dataset/document/task 管理能力，但保持轻量。

- MongoDB 存储管理元数据。
- 新增 dataset：
  - 表示知识库容器，例如“设备运维知识库”。
- 新增 document：
  - 记录上传文件、解析状态、索引状态、chunk 数量、错误信息。
- 新增 task：
  - 记录导入、解析、重建、删除等异步任务状态。
- 导入链路每个节点推进时更新 task/document 状态。
- 服务重启后仍能查询历史任务状态。
- FastAPI lifespan 启动时幂等收口遗留的 `pending/processing` task：已完成 document 对应的漏写 task 修复为完成，其余失去进程内执行者的任务标记为失败并写入 `error_code=import_service_restarted`。
- 前端只把后端持久终态当作成功或失败；网络断开显示“状态暂时无法确认”并继续轮询，不由浏览器擅自判失败。

验收标准：

- 上传文档后可以查询文档状态。
- 任务失败能看到失败节点和错误信息。
- 服务中断后遗留的处理中任务会在下次启动时自动收口，旧 task 不得覆盖 document 的新 task 状态。
- 不再只依赖内存 task_utils 作为唯一状态来源。

## 阶段 3.5：用户维度导入历史隔离

目标：在不引入完整登录和权限系统的前提下，让前端用户刷新后仍能看到“我的导入历史”，并避免用户之间互相看到导入记录和失败信息。

- 新增轻量用户上下文：
  - 前端生成并持久化 `user_id`。
  - 请求通过 `X-User-Id` 传给后端。
  - 后续接入真实登录时，可替换为 JWT 中的用户 ID。
- document/task 增加用户归属字段：
  - `owner_user_id`
  - 预留 `tenant_id`
  - 预留 `visibility`
- 阶段 3.5 只隔离导入历史和状态查询，不改变当前查询召回链路。
- 导入 API 按用户隔离：
  - `POST /upload` 创建 document/task 时写入当前用户。
  - `GET /documents` 只返回当前用户的文档。
  - `GET /documents/{document_id}` 只允许查看当前用户自己的文档。
  - `GET /documents/{document_id}/tasks` 只允许查看当前用户自己的任务历史。
  - `GET /status/{task_id}` 只返回当前用户自己的任务状态。
- 前端补“我的导入记录”：
  - 页面刷新后按当前 `user_id` 拉取历史 document。
  - 对非开发人员展示友好失败原因，技术节点名作为详情信息保留。
- 不做历史数据迁移；当前 Mongo 和 Milvus 只有少量测试数据，可以删库重建。

验收标准：

- 用户 A 上传的文档和失败记录，用户 B 在文档列表和状态接口中看不到。
- 前端刷新后，用户 A 仍能看到自己的历史导入记录和失败摘要。
- 未传用户标识时有明确兜底策略，不能把所有用户混到同一个公开历史里。
- 不引入完整权限、多租户、登录注册系统，但字段设计能承接后续扩展。
- 查询召回是否按用户隔离放到阶段 5，在阶段 4 的 chunk 用户字段落地后再实现。

## 阶段 4：导入链路重构

目标：让“导入后可查、可删、可重建”稳定成立。

保留主流程：

`entry -> pdf_to_md -> image_enrich -> split -> subject_recognition -> embedding -> index`

改造点：

- 导入开始创建 document 和 task。
- PDF/MinerU 解析结果记录到 document 元数据。
- Markdown 图片增强继续使用 MinIO，但图片 URL 关联 document。
- chunk 生成时写入：
  - `dataset_id`
  - `document_id`
  - `owner_user_id`
  - `tenant_id`
  - `visibility`
  - `subject_id`
  - `chunk_index`
  - `enabled`
  - `source_title`
  - `content`
- 入库幂等不再按 `file_title` 删除旧数据，改为按 `document_id` 替换旧 chunk，并用 `index_version` 追踪重建版本。
- 支持删除文档时同步禁用或删除相关 chunk。
- 删除文档和重建索引只能作用于当前用户有权限的 document，避免跨用户误删或重建。
- 阶段 4 只负责把用户归属和可见性字段写入 chunk，不改变查询召回过滤逻辑；正式查询权限过滤放到阶段 5。

验收标准：

- 已有 document 重建索引时不会残留旧 chunk。
- 删除文档后查询不会召回旧 chunk。
- 文档重建索引后 chunk 版本可追踪。
- chunk 能保留用户归属和可见性字段，为阶段 5 查询权限过滤做准备。

当前进度：阶段 4 已完成，包括 document 级入库替换、删除/重建索引 API、Milvus/MinIO 清理、Mongo 软删除、用户隔离、前端轻量操作入口和阶段回归测试。查询召回权限过滤仍按计划放到阶段 5。

## 阶段 5：查询链路重构

目标：让查询链路更稳定、更可解释，并建立后续 Model Planner/GRPO 可以复用的 Action、Observation、Trace 和规则基线。

主流程：

```text
subject_confirm
    -> QueryPlanner 选择 Action
    -> 执行 Action
    -> 返回结构化 Observation
    -> QueryPlanner 根据新状态再决策
    -> answer / ask_clarification / refuse
```

决策边界：

- 单次动作分类只根据原始问题从固定 Action 中选一个，没有执行后的 Observation 和后续决策，使用 SFT 或普通分类即可学习。
- 本项目的 Planner 是多步策略：先选择检索动作，再观察召回数量、分数、精确标识命中和引用候选，然后决定回答、继续 HyDE/Web、追问或拒答。
- Planner 同时使用两类信息，但职责不同：初始决策根据原始问题、主体结果和实时性规则选择第一个 Action；明显实时的问题可以直接 `web_search`，普通设备知识问题默认先 `local_search`。HyDE 和非实时 Web fallback 不在初始步骤一次性预选，而是在 local/HyDE 返回结构化 Observation 后逐步决定是否执行。
- 每次新增检索 Action 后，从所有“已经实际执行”的原始排名列表一次性重算跨 Action 融合：可以只有 original，也可以是 original + HyDE、original + Web、original + HyDE + Web，或实时问题直接只有 Web。不能把上一次 RRF 的输出继续作为下一层 RRF 输入。
- GRPO 优化的是整条 Action Trace 的最终召回、引用、答案、步数和调用成本，而不是六个 Action 之间的单次分类准确率。

改造点：

- 新增统一 `QueryPlanner` 接口和结构化 `PlannerDecision`，接口和检索节点不得依赖具体规则实现。阶段 5 只实现确定性的 `RuleBasedPlanner`，不实现模型训练。
- 固定第一版 Action 集合：
  - `local_search`：检索本地知识库，底层可使用 dense、learned sparse、BM25 和结构化过滤。
  - `hyde_search`：生成假设性答案后再次检索本地知识库。
  - `web_search`：只在本地知识不足或问题具有实时性时调用外部搜索。
  - `answer`：证据充分，进入现有答案生成节点；该 Action 本身不是最终答案文本。
  - `ask_clarification`：信息或主体存在可解决的歧义，需要用户补充。
  - `refuse`：证据不足、超出范围或无法可靠回答时结束流程。
- Planner 输出使用受约束的结构化数据，例如 `action`、`query`、`reason_code`，由 LangGraph 路由到现有执行节点。
- `QueryGraphState` 增加 Planner/Observation 契约：
  - `trace_id`
  - `planner_step`
  - `planner_action_history`
  - `retrieval_observation`
  - `policy_version`
- `retrieval_observation` 只保存可验证的结构化信息，例如召回数量、top rerank 分数、精确标识命中、候选引用、历史 Action、耗时和错误；不记录模型私有思维链。
- 用户问题中提取的 `query_identifiers` 必须忠实保留原始规范化编号，不能被 embedding 找到的相近编号静默覆盖。Observation 需要区分精确命中、metadata 缺失后的同编号命中、待用户确认的不同编号候选和未找到。
- 精确编号过滤为 0 时可以在相同权限、dataset、subject 和 enabled 范围内放宽结构化条件做候选发现；只有仍命中用户原编号的证据可以回答，不同编号只能生成 `ask_clarification`，不能进入 Citation。
- 设置最大 Planner 步数和合法 Action 转移，避免 `local -> hyde -> web -> hyde` 等无限循环。
- `RuleBasedPlanner` 同时承担线上默认策略、阶段 8 评测 baseline 和阶段 9 Model Planner 的故障回退。
- Web search 不再默认并发进入主路径。
- Web search 作为 fallback：
  - 本地知识库召回为空。
  - rerank 分数低于阈值。
  - 用户问题明显需要实时信息。
- 检索时过滤：
  - `dataset_id`
  - `subject_id`
  - `enabled=true`
  - `visibility=public/shared` 或 `owner_user_id=当前用户`
- RRF 兼容单路召回，不因 HyDE 为空直接失败。
- RRF 分两层：单个本地 Action 内融合 dense/learned sparse/BM25；跨 Action 层对 original、HyDE、Web 中所有已经实际执行的原始排名列表统一融合。Web 未执行时不得创建空的伪结果列表；Web 已执行时必须进入累计证据池和统一 rerank。
- 跨 Action 层每次都从各 Action 原始排名重算一次 RRF，不执行“先融合 original + HyDE，再拿融合结果与 Web 套第二次 RRF”的嵌套融合。这样保留每个 Action 的原始名次和来源，避免顺序相关的隐式权重。
- answer 输出引用信息：
  - `document_id`
  - `chunk_id`
  - `title`
  - `source`
  - `score`
- 每次查询记录结构化 Action Trace：
  - 原始/改写 query 和标准主题。
  - 每一步输入 Observation、Planner Action 和 `reason_code`。
  - 各召回通道命中的 chunk、rerank 分数和最终引用。
  - 是否调用 HyDE/Web、总步骤、延迟和失败信息。
  - 当前检索配置、索引版本、`policy_version`、`planner_type`、`provider`、`model_id`、`model_revision` 和 `prompt_version`。
  - Planner 和答案模型各自的 token 用量、耗时和估算成本，便于后续分离对比策略成本与答案生成成本。
- 补充设备运维检索专项评测：
  - 先准备真实设备运维评测集，覆盖报警码、设备型号、SOP 编号、零件编号、中文部件名等典型查询，例如 `E021/E020`、`HAK180/HAK-180/HAK 180`。
  - 查询入口对设备型号、报警码、SOP 编号等做规则提取和归一化。
  - 优先使用阶段 4 已落地的结构化字段做精确过滤或显著加权；精确编号类信息不能只依赖全文检索或 embedding 自行理解。
- 增加 BM25 通道作为阶段 5 的对比实验，而不是直接替换当前 learned sparse：
  - 改造前 `sparse_vector` 语义上是 BGE-M3 的 learned sparse；阶段 5 第七部分已在 chunk collection 中明确改名为 `learned_sparse_vector`。
  - 新增 `lexical_text` 作为 BM25 文本源，组合归一化后的 `content`、标题、`source_title`、设备型号、报警码、零件名、SOP 类型等字段。
  - 新增 Milvus BM25 生成的稀疏向量字段，例如 `bm25_sparse_vector`；应用侧只写 `lexical_text`，不自行计算 BM25 向量。
  - `lexical_text` 需要配置和验证 Analyzer，重点验证中英文混合、连字符、空格、大小写、设备编号和报警码的分词结果。
  - 对报警码、型号、SOP 编号这类精确标识，仍保留结构化字段作为过滤或加权依据，BM25 只作为召回通道之一。
- 查询链路支持可切换的召回组合：
  - `dense + learned_sparse`
  - `dense + BM25`
  - `dense + learned_sparse + BM25`
  - 三路融合不能直接混加 dense、learned sparse、BM25 的原始分数，应使用 RRF 或归一化后的加权融合；当前未归一化的 `WeightedRanker` 不作为三路对比的最终依据。
- 评测时保持相同 chunk、相同 query、相同权限过滤、相同 top-k 和 rerank 配置，对比：
  - `recall@k`
  - `MRR`
  - `nDCG`
  - 引用命中率
  - 召回延迟
  - 索引大小
  - 入库耗时
- 评测结论用于最终 schema 收口：
  - 如果 `dense + BM25` 明显更好，则移除 learned sparse 生成和检索链路。
  - 如果 `dense + learned_sparse` 更好，则移除 BM25 字段和 Function。
  - 如果三路融合有稳定收益，则保留三路。
  - 如果效果接近，优先选择维护成本更低、链路更简单的方案。
- 阶段 5 重写 Milvus schema 时不做历史数据迁移：
  - 当前 Milvus 中只有少量测试数据，允许删除受影响的 collection 后重建，再重新导入测试文档。
  - 不实现旧 schema 在线兼容、历史 backfill、双写迁移、灰度切换或回滚迁移逻辑。
  - 评测期间可以多次删 collection 重建，以保证 schema 和评测数据一致。

验收标准：

- 只用知识库也能稳定回答。
- 查询可以召回公共知识库内容和当前用户自己的私有文档，不召回其他用户私有文档。
- 低质量召回时能拒答或触发 fallback。
- 答案能展示引用来源。
- 同一输入在固定数据和配置下，`RuleBasedPlanner` 可以稳定重放出相同 Action Trace。
- Action、Observation、Trace 都是结构化契约，后续替换为 Model Planner 时不需要改写检索节点。
- 超过最大步骤、输出非法 Action 或 Planner 异常时能回退到规则策略或安全拒答。
- 设备运维评测集能对比 `dense + learned_sparse`、`dense + BM25`、`dense + learned_sparse + BM25` 的效果差异。
- schema 确有调整时通过删除受影响 Milvus collection 并重新导入完成，不引入历史数据兼容逻辑；最终 schema 与评测 collection 相同时只做一致性核对。

当前进度：**阶段 5 已完成。** 查询图按照 `Planner -> 单个 Action -> 累计融合/rerank -> Observation -> Planner` 闭环运行，Web 不再默认并发；`retrieval_traces` 会增量保存去正文后的 Planner/Action/Observation 轨迹、模型用量、终态、Citation 和真实配置快照。Citation 由最终 rerank 证据确定性生成，已贯通同步响应、SSE FINAL、轮询、聊天历史和聊天页。60 条正式评测样本（50 条核心 + 10 条口语）已在 12 份 PDF、11 个设备主体、1806 个 chunk 上完整运行 4 轮；C（三路融合）在核心集四轮 Recall/引用命中率均为 1.0，因此最终选择 `dense_learned_sparse_bm25`，配置版本升级为 `retrieval-stage5-final-v1`。前三轮为三个终端并发压力运行，第四轮为单进程延迟基线。当前 collection 已经是 C 所需三路 schema，不做无差异重建；索引字节数和 Milvus 单独写入耗时无法从现有记录隔离，报告已明确限制且没有估算。0.75 证据阈值保持当前运行基线，本次检索排名评测不宣称已完成答案级阈值优化。聊天历史完整用户隔离仍按计划属于阶段 7。

## 阶段 6：Chunk 可观测与启停

目标：学习 RAGFlow 的 chunk 可视化和人工干预能力，但第一版不做复杂编辑。

- 新增 chunk 列表 API。
- 支持按 document 查看 chunk。
- 支持 chunk 启用/禁用。
- chunk 列表、启用、禁用按当前用户可见范围过滤。
- 查询时只召回 enabled chunk。
- 记录 chunk 启停的操作人、时间和原因，区分解析错误、页眉页脚、乱码、内容过期和人工误判等类型。
- Retrieval Trace 能展示候选 chunk 是否 enabled、在哪个召回通道命中、是否进入 rerank、是否成为最终引用。
- chunk 禁用记录可以作为后续检索评测和训练数据筛选的弱质量信号，但未经人工确认不能直接作为 GRPO reward。
- 评测和 Trace 记录关联 `document_id`、`chunk_id`、`index_version`，避免重建索引后把旧 chunk 标注错误地套到新版本。
- shared/public chunk 的编辑、启停权限策略留到阶段 7 决定。
- 暂不支持修改 chunk 正文和重新向量化。

验收标准：

- 可以禁用解析错误、页眉页脚、乱码 chunk。
- 禁用后查询不再召回该 chunk。
- 用户不能查看或操作其他用户私有文档下的 chunk。
- chunk 保留原始记录，便于后续扩展编辑能力。
- chunk 启停历史可审计，并能被阶段 8 数据构建工具读取。

当前进度：**阶段 6 已完成。** 当前环境验证 Milvus `upsert` 不能安全保持 auto_id
`chunk_id`，因此启停选择路线 B：Milvus 原始 chunk 不改，Mongo `chunk_status_events`
记录审计流水，`chunk_status_overrides.manual_status` 保存当前人工覆盖层。Chunk 管理 API
已支持按 document 列表、详情、启停和事件历史；查询侧保留 Milvus `enabled == true`，
并额外追加 Mongo disabled ID 黑名单 `chunk_id not in [...]`，禁用后不再召回。Retrieval
Trace 已记录候选 `enabled`、召回通道、是否进入 rerank、是否成为 Citation；导入历史页已
提供轻量 Chunk 查看和启停操作入口。阶段 6 不做 chunk 正文编辑、不重新向量化，shared/public
chunk 的最终启停权限策略仍留到阶段 7。

## 阶段 7：管理 API 优先补齐

目标：不先做复杂页面，先把后端管理能力补全。

新增 API：

- Dataset：
  - 创建 dataset
  - 查询 dataset 列表
  - 查询 dataset 详情
  - 支持 dataset owner/visibility 的轻量管理
  - 支持 dataset members 的轻量管理，为 shared/private/public 策略做准备
- Document：
  - 上传文档
  - 查询文档列表
  - 文档列表默认按时间倒序，最新导入记录在最上面
  - 查询文档状态
  - 支持 document owner/visibility 的轻量管理
  - 删除文档
  - 支持清理当前用户不需要的失败导入记录
  - 同名文件的失败记录可以在列表中折叠，或标记为“历史记录”，避免用户修复后重新导入成功时被旧失败记录干扰
  - 重建索引
  - 收紧 `/status/{task_id}` 内存回退策略：只有内存任务记录中存在 `owner_user_id` 且与当前用户匹配时才返回状态；缺少归属信息或 owner 不匹配时返回 404
- Chunk：
  - 阶段 6 已提供按 document 查看 chunk、详情、启停和事件历史。
  - 阶段 7 如继续扩展，重点是跨 document 管理筛选、shared/public 启停权限策略、批量操作和更完整的管理台 API，而不是重做阶段 6 基础接口。
- Conversation：
  - `chat_message` 增加 `user_id`
  - 历史消息查询从仅按 `session_id` 升级为按 `user_id + session_id`
  - 查询当前用户会话列表
  - 查询当前用户某个会话详情
  - 支持删除或隐藏当前用户的会话记录
- Retrieval Test：
  - 输入 query
  - 支持指定固定用户、dataset、检索配置和 `planner_mode=rule`。
  - 返回 `trace_id`、主体识别结果、Planner Action Trace、召回 chunk、rerank 分数、最终引用、耗时和调用成本。
  - 返回并保存 `planner_type`、`provider`、`model_id`、`model_revision`、`prompt_version`、token 用量和分项成本，保证不同 Planner baseline 可复现对比。
  - 支持基于 `trace_id` 重放，重放时明确语料/索引版本和配置是否与原 Trace 一致。
  - 支持为 Trace 写入人工反馈：期望主体、期望 Action、期望 chunk、答案要点、是否应追问/拒答/调用 Web。
- Planner：
  - 查询当前 Planner 模式、策略版本和规则配置摘要。
  - 第一版只允许线上启用 `rule`，为阶段 8-9 的离线评测预留可注册的 `api`、`local_base`、`sft` 和 `grpo` 实现，不在本阶段加载训练模型。
  - 评测框架只允许选择已注册的 Planner 实现；未注册模式必须显式报错，不得静默切换模型。
- Trace 与反馈接口只保存结构化决策和结果，不保存或对外返回模型私有思维链。

验收标准：

- 面试演示时可以通过 API 展示完整 RAG 管理闭环。
- 可以区分公共 demo 知识库和用户私有导入内容。
- 用户只能查看自己的聊天会话和消息历史。
- Retrieval Test 可以完整展示并重放规则 Planner 的决策过程。
- 人工反馈能与 Trace、文档版本和 chunk 版本稳定关联，为阶段 8 构建数据集提供来源。
- 前端页面可以后续基于这些 API 再补。

## 阶段 8：评测与 Demo 数据

目标：让项目从“能跑”变成“能证明效果”，并准备阶段 9 所需的离线环境、数据拆分和 Reward 契约；本阶段不执行 GRPO 训练。

- 保留 10-20 个高质量设备运维问题作为面试 Demo 和快速回归集，不把这组小样本直接用于 GRPO 训练。
- 另外构建规模显著更大、类型分布更完整的 Planner 训练候选池，可由真实问题、文档约束下的合成问题和人工改写组成；关键样本必须人工复核。
- 区分公共 demo 知识库评测和用户私有文档评测。
- 每个问题标注：
  - 标准主题
  - 期望 chunk
  - 期望答案要点
  - 是否应该拒答
  - 是否应该追问
  - 是否需要 Web 实时信息
  - 可接受或期望的 Action 路径
- 数据按 train/dev/test 拆分；测试集不参与规则调参、Reward 权重调节或阶段 9 训练，并尽量按来源文档隔离以减少泄漏。
- 固化可重放的离线 RAG Environment：
  - 固定 dataset、document/index version 和 enabled chunk 快照。
  - 固定检索组合、top-k、reranker 和 Planner 配置。
  - 接收外部传入的结构化 Action，复制独立 State 执行并返回 Observation。
  - 对相同初始 State 的多条轨迹分别计算 Reward 明细。
- 在相同 dev/test 数据、知识库快照、检索配置和答案模型下建立三类 Planner baseline：
  - `RuleBasedPlanner`：确定性的最低可用基线和线上回退策略。
  - 顶级 API Planner：作为强能力上限参考和 teacher 候选，不假设本地模型天然更优。
  - 本地开源模型零样本 Planner：在未训练状态下评估结构化输出、基本路由、延迟和部署成本。
- 顶级 API 产生的轨迹不能直接视为标准答案；只有通过 Environment Reward 筛选或人工复核的轨迹才能进入训练数据。
- 从高质量规则轨迹、通过筛选的 API 轨迹和人工标注中构建 SFT 数据，用于学习结构化格式、基本路由和合法状态转移。
- 本地 Planner 模型暂不在方案阶段锁死具体 checkpoint：
  - 首选候选为 [Qwen3.5-4B](https://huggingface.co/Qwen/Qwen3.5-4B)，优先验证其小模型训练和本地部署性价比。
  - [Qwen3.5-9B](https://huggingface.co/Qwen/Qwen3.5-9B) 作为 4B 能力不足时的升级对照，不默认直接使用更大模型。
  - 阶段 8 先完成训练框架、结构化输出、非思考模式和本地推理兼容性测试，再冻结 `model_id`、revision、tokenizer、chat template 和量化配置。
- 定义阶段 9 使用的 Reward v1：
  - `R_format`：Planner 输出能否通过结构化 schema 和 Action 校验。
  - `R_retrieval`：期望 chunk 的 recall、MRR、nDCG 和关键标识命中。
  - `R_citation`：最终引用是否来自期望/有效 chunk。
  - `R_answer`：答案要点覆盖和事实一致性。
  - `R_behavior`：追问、拒答、Web fallback 是否符合标注。
  - `R_cost`：不必要 HyDE/Web、过多步骤、延迟和调用成本惩罚。
- Reward 优先使用可执行、可复现的规则指标；LLM Judge 只能作为补充，并保存各分项而不是只保存一个总分，便于发现 reward hacking。
- 评测指标：
  - `recall@k`
  - `MRR`
  - `nDCG`
  - 引用命中率
  - 拒答率
  - 追问正确率
  - 不必要 HyDE/Web 调用率
  - 平均 Planner 步数、延迟和调用成本
  - Reward 各分项和总分
- 输出评测报告，作为面试展示材料。
- 运行规则、顶级 API 和本地零样本 Planner baseline 并固化结果，阶段 9 必须在同一 held-out 测试集和环境快照上对比。

验收标准：

- 可以一键跑评测。
- 能看到重构前后召回和引用质量变化。
- Demo 能围绕设备手册、报警码、维护 SOP 展示。
- 评测时明确使用固定测试用户，避免用户隔离导致召回范围不稳定。
- Demo 回归集、Planner 训练集和 held-out 测试集边界清晰，不用测试集生成或挑选训练轨迹。
- 离线环境可以执行指定 Action 序列并产出完整 Observation、Trace 和 Reward 明细。
- 规则、顶级 API 和本地零样本 Planner baseline、数据版本、配置和评测报告均可复现。
- API teacher 的低 Reward 或未复核轨迹不会进入 SFT 数据，held-out test 集不用于选择模型、Prompt、Reward 权重或训练轨迹。

## 阶段 9：基于 SFT + GRPO 的 Query Planner 策略优化

目标：在阶段 5 的规则 Planner 和阶段 8 的离线评测环境之上，通过 SFT 建立结构化决策基线，再使用 GRPO 优化多步检索轨迹，验证强化学习相对 SFT 是否存在真实增益，而不是仅为项目增加 GRPO 技术标签。

训练顺序：

```text
本地开源基础模型
    -> SFT：学习 PlannerDecision 格式、基本路由和合法状态转移
    -> GRPO：优化多步轨迹的召回、引用、答案、延迟和成本综合收益
```

范围与边界：

- 线上运行的是 `ModelPlanner`，GRPO 只在线下训练流程中执行；不把 GRPO Trainer 放进每次查询的 LangGraph 节点。
- 第一版只训练 Planner Policy，不训练最终答案生成模型、embedding 或 reranker。
- 当前 OpenAI 兼容 API 客户端只承担推理；训练侧单独选择可训练的开源权重模型、训练框架、checkpoint 和推理适配层。
- Policy 在底层仍生成 token，但业务输出必须解析成 `PlannerDecision`，Action 集合沿用阶段 5 契约。
- 先使用阶段 8 已筛选的规则轨迹、API teacher 轨迹和人工标注执行 SFT；SFT 是正式实验阶段和必须保留的对照组，不再只是基础模型输出无效时的可选格式热身。
- 同一个初始 State 使用较高温度采样一组不同 Planner 输出；每条 rollout 使用独立复制的环境 State 执行，不能通过 Python 遍历 `if/else` 代替模型采样。
- 多步 rollout 持续执行“Policy 生成 Action -> Environment 返回 Observation”，直到 `answer/ask_clarification/refuse` 或达到最大步骤。
- 训练时保存生成 token 的概率、结构化 Action Trace 和 Reward 明细，使高 Reward 轨迹能够反向优化 Policy。
- GRPO 使用阶段 8 的多项 Reward 加权，持续检查无意义增加检索次数、滥用 Web、输出冗长和伪造工具结果等 reward hacking。
- 增加 `planner_mode=rule|model`、`policy_version` 和 feature flag；Model Planner 超时、输出非法 Action 或服务不可用时回退 `RuleBasedPlanner`。
- 训练完成后使用本地 vLLM 或 SGLang 提供 OpenAI 兼容推理接口，线上 Planner 关闭思考模式并只生成简短的结构化决策。
- 顶级 API 主要用于离线 baseline 和 teacher，不作为线上 Planner 的必需依赖；线上异常优先回退可复现的 `RuleBasedPlanner`。
- 不进行线上实时强化学习。用户 Trace 和反馈先离线脱敏、审核、版本化，再进入后续训练批次。

对比实验：

- 在相同 held-out 测试集、知识库快照、检索配置、答案模型和 Reward 版本下对比：
  - `RuleBasedPlanner`
  - 顶级 API Planner
  - 未训练的基础 `ModelPlanner`
  - SFT Planner
  - GRPO Planner
- 对比召回、引用、答案要点、追问/拒答正确率、不必要 HyDE/Web 调用率、平均步骤、延迟和成本。
- GRPO 必须相对 SFT Planner 在 held-out 核心指标上产生稳定增益，且不以滥用 HyDE/Web、增加步骤或延迟换取表面 Reward；否则保留 SFT 或规则 Planner，并明确记录 GRPO 在本场景没有必要。
- 本地 Planner 不要求绝对超过顶级 API 的原始质量，但评测报告必须同时说明质量差距、延迟、边际调用成本、本地部署和数据不出内网的价值。

验收标准：

- 同一 query 可以由 Policy 采样出多条合法但不同的 Action 轨迹，环境能够独立执行并计算相对 Reward。
- 同一问题可以在不同 Observation 下稳定产生 `local -> answer`、`local -> hyde -> answer` 等不同的合法多步轨迹。
- 训练、评测、模型 checkpoint、Policy/Reward/数据版本和随机种子可追踪。
- Model Planner 接入时不修改既有检索、HyDE、Web、答案节点，只替换 `QueryPlanner` 实现。
- `planner_mode` 可灰度切换并一键回退规则 Planner。
- held-out 报告能够回答 GRPO 相比规则、顶级 API、本地零样本和 SFT Planner 改善了什么、付出了多少延迟/成本，以及是否值得保留。

## 非目标

第一版不做以下内容：

- 完整权限、多租户。
- 复杂前端管理台。
- Word、Excel、网页同步等多数据源。
- chunk 正文编辑和自动重新向量化。
- 完整复刻 RAGFlow 的 parser/chunk 模板系统。
- 分布式任务队列。
- 在线实时强化学习和未经审核的用户反馈自动训练。
- 第一版 GRPO 训练最终答案生成模型或保存模型私有思维链。

## 推荐实施顺序

1. 阶段 0：项目基线稳定。
2. 阶段 1：统一状态契约与分层边界。
3. 阶段 2：主题体系重构。
4. 阶段 3：文档与任务元数据闭环。
5. 阶段 3.5：用户维度导入历史隔离。
6. 阶段 4：导入链路重构。
7. 阶段 5：查询链路重构。
8. 阶段 6：Chunk 可观测与启停。
9. 阶段 7：管理 API。
10. 阶段 8：评测与 Demo 数据。
11. 阶段 9：基于 SFT + GRPO 的 Query Planner 策略优化。
