# 后续优化

## Reward v1.1：按证据强度优化检索奖励

### 背景

阶段 8.5 当前实现的 Reward v1 已经能对 Planner 轨迹做可复现评分，但检索分仍然是第一版规则：

- `expected_chunks` 会被统一成 `(document_id, chunk_id, index_version)`。
- `recall@k`、`MRR`、`nDCG@k` 当前按二值相关性计算。
- `ExpectedChunk.relevance` 中的 `required`、`supporting`、`acceptable` 暂未实际参与打分。

这意味着当前 Reward v1 可以判断“是否命中 expected chunk”和“命中位置是否靠前”，但还不能充分表达“核心证据排得太靠后比可替代证据排得太靠后更严重”。

### 优化目标

Reward v1.1 需要让检索分同时考虑三件事：

1. 证据强度：`required` 必须比 `supporting`、`acceptable` 更重要。
2. 排名位置：同一个证据被排到第 1 位和第 10 位不能同分。
3. 核心证据硬约束：`required` 没召回或排名过低时，检索分应设置明显上限。

目标不是简单地写成“召回后按 `required=3`、`supporting=2`、`acceptable=1` 加分”，因为这种方式会忽略排名质量。更合理的设计是：

```text
最终检索奖励 = 强度加权召回 + 强度加权排序分 + required 核心证据门槛
```

### 建议设计

#### 1. 增加强度权重表

建议在 `app/rag/evaluation/metrics.py` 或 `RewardConfig` 中定义默认权重：

```text
required    = 3.0
supporting  = 2.0
acceptable  = 1.0
```

含义：

- `required`：必须命中的核心证据，未命中会明显影响 `R_retrieval` 和 `R_citation`。
- `supporting`：支撑性证据，命中会提高答案可靠性，但通常不应决定整条轨迹失败。
- `acceptable`：可接受替代证据，适合同一答案要点存在多个等价 chunk 的场景。

#### 2. 实现 weighted_recall

当前 `recall@k` 是：

```text
命中的 expected chunk 数量 / expected chunk 总数
```

v1.1 建议改为：

```text
命中的 expected chunk 权重之和 / expected chunk 权重总和
```

这样同样漏掉一个 chunk 时：

- 漏掉 `required` 扣分更重。
- 漏掉 `acceptable` 扣分更轻。

#### 3. 实现 weighted_nDCG

当前 `nDCG@k` 使用二值相关性：

```text
命中 expected chunk = 1
未命中 expected chunk = 0
```

v1.1 建议改为 graded relevance：

```text
required    -> gain = 3
supporting  -> gain = 2
acceptable  -> gain = 1
```

并继续保留排名折损：

```text
DCG = sum(gain / log2(rank + 1))
```

这样可以表达：

- `required` 排第 1 位明显优于排第 10 位。
- `acceptable` 排第 10 位也会扣分，但扣得比 `required` 轻。
- `required + supporting + acceptable` 的理想排序应该优先把 `required` 放前面。

#### 4. 增加 required_gate / required_cap

仅靠 weighted_nDCG 仍不够，因为某些样本 expected chunk 数量较少时，归一化后的差异可能不够明显。

建议增加核心证据门槛：

```text
如果 required 一个都没召回：
    retrieval_score <= 0.30

如果 required 已召回但没有进入 top3：
    retrieval_score <= 0.60

如果 required 已召回但没有进入 top5：
    retrieval_score <= 0.80
```

具体阈值可先作为 `RewardConfig` 参数，后续根据 dev/test 报告调节。

#### 5. 保留原始分项明细

Reward v1.1 输出中不要只保留一个总分，应继续保留机器可读明细：

```text
weighted_recall_at_k
weighted_ndcg_at_k
required_hit_count
required_total_count
required_best_rank
required_gate_cap
relevance_weights
```

这样后续排查 Reward hacking 时可以知道模型到底是：

- 没找到 required；
- 找到了但排太后；
- 只找到了 acceptable；
- 靠大量低价值 evidence 把表面分数堆高。

### 对阶段 9 GRPO 的意义

阶段 9 的 GRPO 会直接使用 `TrajectoryReward.total_reward` 作为训练奖励。如果 Reward 不能区分证据强度，模型可能学到一种表面策略：

```text
召回很多 acceptable/supporting chunk
但 required chunk 排得很靠后
仍然拿到不低分数
```

Reward v1.1 的目标就是避免这种问题，让 GRPO 更明确地优化：

- 优先找到核心证据；
- 核心证据尽量靠前；
- 支撑证据作为补充；
- 可替代证据不抢核心证据的位置；
- 不通过多走 HyDE/Web 或堆候选来掩盖核心证据质量问题。

### 建议实施顺序

1. 在 `metrics.py` 中新增带强度的 expected chunk 数据结构或 helper。
2. 保留当前二值指标函数，新增 `weighted_recall_at_k` 和 `weighted_ndcg_at_k`，避免破坏历史报告。
3. 在 `reward.py` 的 `score_retrieval` 中切换到 v1.1 检索公式。
4. 在 `RewardConfig` 中增加 relevance 权重和 required gate 阈值。
5. 更新 Reward 明细字段，确保报告能展示 required/supporting/acceptable 的命中和排名。
6. 增加单元测试覆盖以下场景：
   - `required` 排第 10 位时扣分明显重于 `acceptable` 排第 10 位。
   - `required` 未召回时触发 retrieval 分上限。
   - `supporting` 和 `acceptable` 命中可以加分，但不能抵消 required 缺失。
   - 没有 expected chunk 的拒答/追问样本不被检索分误扣。
7. 用同一批 stage8 dev/test 快照重新生成评测报告，对比 Reward v1 和 v1.1 的分项变化。

### 暂不进入本轮的内容

- 不在阶段 8.5 直接重新训练 Planner。
- 不把 LLM Judge 作为主奖励替代规则指标。
- 不把未人工审核的新制造业公开数据直接混入 SFT/GRPO。
- 不为了提升检索分鼓励无意义增加 HyDE/Web 调用；成本分仍需要保留惩罚。
