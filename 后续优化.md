# 后续优化

## Reward v1.1：按 Planner 可控性重标定六路权重

### 背景

阶段 8 当前实现的 Reward v1 已经能对 Planner 轨迹做可复现评分，六路分项包括：

- `format`：结构化格式和 Action 合法性。
- `retrieval`：expected chunk 的 recall、MRR、nDCG 和结构化标识命中。
- `citation`：最终 Citation 是否引用期望或有效 chunk。
- `answer`：答案要点覆盖或拒答/追问终态正确性。
- `behavior`：是否该答、该拒、该追问、该 Web。
- `cost`：不必要 HyDE/Web、额外步骤、延迟和调用成本。

当前权重是：

```text
format    = 0.15
retrieval = 0.25
citation  = 0.15
answer    = 0.20
behavior  = 0.15
cost      = 0.10
```

这个权重适合阶段 8 离线评测，但不完全适合阶段 9 的 Planner 训练。

原因是 Planner 不直接决定 chunk 排名和 Citation。Planner 直接决定的是检索路线：

- 是否先 `local_search`。
- 是否追加 `hyde_search`。
- 是否调用 `web_search`。
- 什么时候 `answer`。
- 什么时候 `ask_clarification`。
- 什么时候 `refuse`。
- 是否多走无意义步骤。

Planner 只能通过检索路线、fallback 和回答时机间接影响 chunk 与 Citation。如果把 `retrieval + citation` 权重设得过高，GRPO 可能主要优化一个由固定检索器、reranker、chunk 质量和标注决定的间接指标，训练信号会不够干净。

### 优化目标

Reward v1.1 的目标不是重写检索指标，而是让六路权重更符合 Planner 的可控范围：

1. 提高 `behavior` 和 `cost`，把“检索路线分”放到主位置。
2. 降低 `retrieval` 和 `citation`，让它们作为终局质量约束，而不是训练主信号。
3. 保留 `format` 和 `answer` 的中等权重，防止模型格式合法但行为错误，或路线正确但终态/答案要点不合格。
4. 暂不新增 `weighted_recall_at_k`、`weighted_ndcg_at_k` 或 required gate，避免把 chunk 排名细节做成 Planner 训练的高权重目标。

### 建议权重

Reward v1.1 第一版权重：

```text
format    = 0.15
retrieval = 0.12
citation  = 0.08
answer    = 0.15
behavior  = 0.35
cost      = 0.15
```

解释：

- `behavior + cost = 0.50`，作为 Planner 直接可控的检索路线主信号。
- `retrieval + citation = 0.20`，作为证据质量底线和报告指标保留。
- `format + answer = 0.30`，保证结构化输出、终态和答案质量不被忽略。

### 为什么暂不改检索指标

二值 recall、MRR、nDCG 不是完美指标，但当前阶段没必要先改它们。

理由：

- chunk 排名主要由检索器和 reranker 决定，不是 Planner 直接输出。
- `required/supporting/acceptable` 的证据强度有价值，但更适合作为数据标注和报告解释，暂不作为 Planner 训练的高权重主信号。
- 如果现在新增 weighted recall、weighted nDCG 和 required gate，容易把 Reward v1.1 的重点重新拉回 chunk 排名。
- 阶段 9 训练前更重要的是先验证不同 Action path 的 `behavior/cost` 是否能稳定区分好坏路线。

因此，Reward v1.1 暂时保留现有 `score_retrieval` 内部公式，只调整六路聚合权重。

### 训练前验证

阶段 9 真正训练前，需要在 dev case 上验证分项差异。

建议流程：

```text
同一批 dev case
    -> 构造或采样多条 Action path
    -> 分别计算 format/retrieval/citation/answer/behavior/cost
    -> 统计不同路径下各分项方差
    -> 决定最终权重是否需要小幅调整
```

判断标准：

- 如果 `behavior/cost` 能明显区分正确路线和错误路线，保持高权重。
- 如果 `retrieval/citation` 在不同 Planner 轨迹之间几乎不变，继续低权重，只作为评测指标。
- 如果某些问题中 `hyde_search` 或 `web_search` 的路线选择确实显著改善证据质量，可以保留 `retrieval/citation` 的辅助作用，但不升为主信号。
- 如果 `cost` 过高导致模型倾向不检索就拒答，需要降低 `cost` 或提高 `behavior/answer` 对正确回答的约束。

### 建议实施顺序

1. 将 `REWARD_VERSION` 升级为 `reward-v1.1`。
2. 调整 `RewardWeights` 默认值：
   - `format=0.15`
   - `retrieval=0.12`
   - `citation=0.08`
   - `answer=0.15`
   - `behavior=0.35`
   - `cost=0.15`
3. 更新 `RewardWeights` 和 `RewardConfig` 的中文字段说明，明确：
   - `behavior + cost` 是 Planner 直接可控的检索路线分。
   - `retrieval + citation` 是低权重终局质量约束。
   - `score_retrieval` 内部公式暂不调整。
4. 增加单元测试覆盖：
   - Reward v1.1 默认六路权重。
   - 不必要 HyDE/Web 会通过 `behavior/cost` 明显降低总分。
   - `retrieval + citation` 权重低于 `behavior + cost`。
   - 非回答型样本仍不因缺少 expected chunk 被检索分误扣。
5. 用同一批 stage8 dev case 重新生成 `reward_v1_1_baseline_dev.json`。
6. 在阶段 8.5 报告中展示六路分项均值和路线质量差异，不只展示总分。

### 暂不进入本轮的内容

- 不新增 `weighted_recall_at_k`。
- 不新增 `weighted_ndcg_at_k`。
- 不新增 required gate 或 required top3/top5 硬上限。
- 不把 LLM Judge 作为主奖励替代规则指标。
- 不在阶段 8.5 直接训练 Planner。
- 不把未人工审核的新制造业公开数据直接混入 SFT/GRPO。
