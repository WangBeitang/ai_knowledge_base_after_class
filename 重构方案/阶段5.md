# 阶段 5：查询链路重构

## 背景与目标

阶段 4 已经把 chunk 从“文件标题维度”升级为“document 维度”，并写入了：

```text
dataset_id
document_id
owner_user_id
tenant_id
visibility
index_version
chunk_index
enabled
source_title
```

这些字段使查询侧具备了按知识库、用户、可见性和 chunk 状态过滤的基础。但当前查询链路仍有几个关键问题：

- 查询 API 和聊天页还没有传递 `X-User-Id`，`QueryGraphState` 也没有用户和 dataset 上下文。
- chunk 检索目前只按 `subject_id` 过滤，阶段 4 写入的权限字段和 `enabled` 尚未生效。
- LangGraph 在主体确认后固定并发执行普通检索、HyDE 和 Web Search；Web 不是 fallback，查询成本和行为都不可解释。
- 当前图没有统一 Planner 契约，后续接入 Model Planner/GRPO 时会被迫重写图和检索节点。
- 当前 `rrf_service` 只融合普通检索与 HyDE，底层 Milvus 检索仍固定为 dense + BGE-M3 learned sparse 两路 `WeightedRanker`。
- rerank 前的格式转换会丢失 `document_id`、`chunk_id`、`index_version` 等元数据，最终答案无法稳定返回引用来源。
- 查询过程只有节点进度，没有结构化 Action、Observation 和 Retrieval Trace，无法解释为什么调用 HyDE/Web，也无法稳定重放规则基线。
- 设备型号、报警码、SOP 编号、零件编号等精确标识目前主要依赖 embedding/learned sparse，自身没有查询侧提取、归一化和结构化过滤闭环。

阶段 5 的目标不是引入一个“会自由思考的 Agent”，而是把查询流程改造成一个可约束、可解释、可评测、可替换策略的执行环境：

```text
subject_confirm
    -> QueryPlanner 选择 Action
    -> 执行一个合法 Action
    -> 返回结构化 Observation
    -> QueryPlanner 根据新状态再决策
    -> answer / ask_clarification / refuse
```

本阶段需明确区分“单次动作分类”和“多步 Planner”：

- 单次动作分类只根据原始问题从固定 Action 中选一个，没有执行后的 Observation 和后续决策，使用 SFT 或普通分类即可学习。
- 本项目的 Planner 是多步策略：先选择检索动作，再观察召回数量、分数、精确标识命中和候选证据，然后决定回答、继续 HyDE/Web、追问或拒答。
- “根据问题判断”和“根据结果判断”不是二选一：第一次 plan 根据原始问题、主体结果和实时性规则选择首个 Action；后续 plan 必须读取刚执行完成的 Observation 和 Action history，再决定是否继续。明显实时的问题可以直接 `web_search`，普通设备知识问题默认先 `local_search`；HyDE 和非实时 Web fallback 不能在查询开始时一次性预选并并发执行。
- 阶段 9 的 GRPO 优化整条 Action Trace 的召回、引用、答案、步数和调用成本，而不是六个 Action 之间的单次分类准确率。

阶段 5 在该通用接口上只注入 `RuleBasedPlanner`；后续替换为 Model Planner 时不改检索和答案节点。

本阶段完成后，应满足：

- 查询只召回公共/共享内容和当前用户自己的私有内容。
- 默认先查本地知识库，HyDE 和 Web 只在规则满足时触发。
- Planner 决策只输出受约束的 Action、query 和 `reason_code`，不直接生成答案文本。
- 每一步都有结构化 Observation 和 Trace，固定输入、固定 Observation 和固定策略版本下能得到相同 Action Trace。
- answer 返回稳定的引用字段，能追踪到本地 document/chunk 或 Web URL。
- 可以公平比较 dense + learned sparse、dense + BM25、三路融合，并在本阶段内依据评测结果收口最终 Milvus schema。
- 阶段 9 替换 `QueryPlanner` 实现时，不需要修改检索、HyDE、Web、rerank 和答案生成节点。

## 当前实现基线

阶段 5 开始前，查询链路实际是：

```text
node_subject_name_confirm
    -> node_search_embedding ───────────────┐
    -> node_search_embedding_hyde ─────────┼-> node_rrf -> node_rerank -> node_answer_output
    -> node_web_search_mcp ─────────────────┘
```

需要以以下事实为改造起点：

- `QueryGraphState` 目前只有会话、问题、主体、召回结果和答案字段，没有用户、dataset、Planner、Observation、Trace 和 citation 契约。
- 主体候选不明确时，`subject_name_confirm_service` 直接写 `state["answer"]`；阶段 5 要改为输出主体确认结果，由 Planner 决定 `ask_clarification` 或 `refuse`。
- `chunk_retrieval_utils.build_subject_filter_expr` 只生成 `subject_id in [...]`。
- `embedding_search_service` 和 `hyde_search_service` 都调用固定的 dense + `sparse_vector` 请求。
- `milvus_utils.hybrid_search` 固定使用 `WeightedRanker`，还不能根据召回组合切换为 `RRFRanker`。
- `rerank_service.fuse_documents` 会把本地 chunk 转成只有 `title/text/url/type/score` 的结构，导致引用所需的 document/chunk 元数据丢失。
- `rrf_service.check_params` 在本地两路都为空时直接抛错，Planner 没有机会根据空召回选择 Web 或拒答。
- 查询 API、SSE 最终包和非流式响应都没有 `trace_id`、`citations`。
- 聊天页没有生成/复用 `kb_user_id`，查询请求也没有 `X-User-Id`。
- 当前依赖为 `pymilvus 2.6.10`，客户端已经具备 BM25 Function 和 `RRFRanker` 能力，但仍需先确认实际 Milvus Server 版本及 Analyzer 行为。

## 改造边界

### 本阶段要做

- 新增统一 `QueryPlanner` 接口、结构化 `PlannerDecision` 和确定性的 `RuleBasedPlanner`。
- 固化六个 Action：`local_search`、`hyde_search`、`web_search`、`answer`、`ask_clarification`、`refuse`。
- 改造 LangGraph，使其按 Planner 决策串行执行 Action 并回到 Planner。
- 增加最大步骤、合法 Action 转移、重复 Action 检查和安全收口。
- 查询入口补齐 `X-User-Id`、`dataset_ids` 和默认 tenant 上下文。
- 对所有本地检索通道统一执行 dataset、subject、enabled、visibility/owner 过滤。
- 增加设备运维精确标识提取、归一化、精确过滤和零命中降级策略。
- 将现有 `sparse_vector` 明确为 BGE-M3 learned sparse；临时增加 BM25 实验字段和 Function。
- 支持三种可切换的本地召回组合，并统一使用基于名次的 RRF 做通道融合。
- 把 Web Search 从默认并发路径改为规则 fallback。
- 保留候选 chunk 的完整元数据，生成结构化 citations。
- 新增 Retrieval Trace 持久化，记录结构化 Action、Observation、证据标识、版本和耗时。
- 补充阶段 5 专项评测、单元测试、图测试、API 测试和用户隔离测试。
- 评测后删除未保留通道的字段、索引、Function、配置和死代码，重新建 collection 并导入测试文档。
- 同步 `docs/数据字典.md`、根目录 `重构方案.md` 和阶段 5 评测结论。

### 本阶段不做

- 不实现 `ModelPlanner` 推理，不加载 Planner 模型，不执行 SFT/GRPO 训练。
- 不把 LLM 私有思维链、隐藏推理文本或模型 token 概率写入 Trace。
- 不做在线强化学习，不把用户反馈自动用于训练。
- 不做完整 Dataset 管理、成员管理、RBAC 或多租户后台；这些仍属于阶段 7。
- 不做聊天会话列表和完整的 `user_id + session_id` 历史管理；阶段 7 再收口。
- 不做 Trace 查询/重放/人工反馈管理 API；阶段 5 只落数据契约和内部持久化，阶段 7 再开放管理接口。
- 不做 chunk 列表、人工启停和编辑；阶段 6 实现。阶段 5 只在查询过滤中强制 `enabled == true`。
- 不做完整阶段 8 评测平台和大规模训练数据集；阶段 5 只建设决定检索 schema 所需的小型专项评测。
- 不迁移历史 Milvus 数据，不做双写、backfill、在线 schema 兼容或灰度迁移。
- 不把前端改造成复杂的 Planner 调试台，只补用户身份、引用展示和必要的 trace_id 展示。

## 核心设计决策

### 1. Planner 是纯决策层，不包含检索实现

`QueryPlanner` 实现只读取结构化 `PlannerContext`/Observation，返回 `PlannerDecision`。阶段 5 的具体实现是 `RuleBasedPlanner`。Planner 不能：

- 直接访问 Milvus、Mongo、Web 或 LLM。
- 直接生成最终答案文本。
- 修改检索结果。
- 输出自由文本推理过程。

检索和答案仍由现有 service/node 执行。阶段 9 只替换 Planner 实现。

### 2. Observation 只表达可验证事实

Observation 可以包含：

- 本次 Action 是否成功。
- 各通道召回数量。
- 去重后的候选数量。
- rerank 后数量与 top score。
- 命中的设备型号、报警码、SOP/零件编号。
- 可生成的引用数量和有限数量的候选证据摘要。
- 耗时、错误码和是否发生结构化过滤降级。

Observation 不包含“我认为”“我的分析过程”等自由文本思维链。

候选证据摘要是 Model Planner 判断“证据是否真正覆盖问题”的运行时输入，不能只给出数量和分数，否则后续策略只能学会阈值分支。运行时 Observation 可携带受长度限制的证据片段；持久化 Trace 默认只保存证据 ID、标题、分数、命中标识和摘要 hash，不复制大量私有 chunk 正文。

### 3. 规则 Planner 的稳定性以结构化输入为边界

`RuleBasedPlanner.plan(context)` 必须是纯函数：相同 policy/config、相同主体确认结果、相同 Observation、相同 Action history，必须返回相同 `PlannerDecision`。

线上完整查询仍可能受到主体识别 LLM、HyDE 文本、Web 内容和外部服务变化影响，因此“稳定重放”分两层验收：

- Planner 单元重放：直接输入固定 Observation，Action Trace 必须完全一致。
- 固定语料集成重放：固定模型配置、知识库快照、检索配置并禁用 Web，Action 和 `reason_code` 必须一致；最终答案措辞不要求逐字相同。

### 4. Web 是受控 fallback，不再默认并发

Web 仅在以下任一条件满足时允许调用：

- 规则识别到明显实时性词语，例如“最新、今天、当前、实时、刚发布、价格、政策更新”。
- 本地检索为空。
- 本地/HyDE 经过 rerank 后仍低于证据阈值。
- 本地检索执行失败且问题允许外部信息回答。

设备说明书、报警码、维护 SOP 等内部知识问题，不因为一次低分就无条件相信 Web；Web 失败或没有可靠结果时安全拒答。

### 5. `shared` 的阶段 5 语义必须显式限定

阶段 7 尚未实现 dataset members。阶段 5 暂时采用轻量语义：

```text
private -> 仅 owner 可检索
public  -> 所有当前查询用户可检索
shared  -> 同一 tenant 内可检索
```

当前 `tenant_id` 固定为 `tenant_default`，因此 `shared` 实际相当于当前租户内共享。阶段 7 引入成员关系后必须收紧，不能把本阶段表达式当作最终权限模型。

### 6. BM25 是对比实验，不预判胜负

阶段 5A 临时保留三路字段：

```text
dense_vector
learned_sparse_vector
bm25_sparse_vector
```

对比完成后进入阶段 5B schema 收口，只保留有证据支持的组合。若质量接近，优先保留维护成本更低、入库与查询链路更简单的方案。

## 目标调用链与状态机

### 目标 LangGraph

```text
START
  -> subject_confirm
  -> planner
       ├─ local_search
       │    -> local_retrieval -> intra_action_rrf ─┐
       ├─ hyde_search
       │    -> hyde_retrieval  -> intra_action_rrf ─┤
       ├─ web_search
       │    -> web_retrieval                       ─┤
       │                                            ↓
       │    action_result_store
       │      -> cross_action_rrf
       │      -> unified_rerank
       │      -> observation
       │      -> planner
       ├─ answer
       │    -> answer_output -> trace_finalize -> END
       ├─ ask_clarification
       │    -> terminal_response -> trace_finalize -> END
       └─ refuse
            -> terminal_response -> trace_finalize -> END
```

说明：

- `subject_confirm` 不再决定图终点，只输出主体确认状态、候选和提示文本。
- `planner` 只生成决策并校验合法性。
- `observation` 记录刚执行 Action 的通道数量、耗时、错误等执行事实，同时读取所有已执行检索 Action 的累计融合/rerank 结果判断当前证据是否充分，并追加 Action history。
- `local_search` 和 `hyde_search` 使用同一套权限过滤和召回组合。
- Web 结果不进入第一层 Milvus 通道 RRF；它保留自己的原始排名，在第二层跨 Action RRF 中与已执行的 original/HyDE 列表一次性融合，再和本地候选一起进入统一 reranker。
- 任何执行异常都先写 Observation/Trace，再回到 Planner 做 fallback 或安全拒答；不允许无限重试。

### 合法 Action 转移

| 当前阶段/上一个 Action | 允许的下一个 Action |
|---|---|
| 主体确认完成 | `local_search`、`web_search`、`ask_clarification`、`refuse` |
| `local_search` 完成 | `answer`、`hyde_search`、`web_search`、`ask_clarification`、`refuse` |
| `hyde_search` 完成 | `answer`、`web_search`、`ask_clarification`、`refuse` |
| `web_search` 完成 | `answer`、`ask_clarification`、`refuse` |
| `answer` | 终止 |
| `ask_clarification` | 终止 |
| `refuse` | 终止 |

额外约束：

- 同一个检索 Action 一次查询最多执行一次。
- `answer`、`ask_clarification`、`refuse` 必须是终止 Action。
- 默认最大 Planner 决策步数为 4；配置必须进入 Trace。
- 超过最大步数、非法转移、未知 Action、Planner 异常都转成 `refuse`，`reason_code=SAFE_GUARD_TRIGGERED`。
- 阶段 9 的 Model Planner 输出非法时回退规则 Planner；阶段 5 先实现相同的校验入口和回退接口测试。

## 具体改造任务

### 1. 固化查询输入、身份和知识库范围

目标：让查询在进入图之前就拥有明确的用户、tenant 和 dataset 上下文。

细分任务：

- 查询 API 复用导入 API 的轻量身份规则，强制读取：

```http
X-User-Id: user_xxx
```

- 缺少或只有空白 `X-User-Id` 时返回 400，不自动合并到公共 `anonymous_user`。
- `QueryRequestParam` 增加可选 `dataset_ids: list[str]`。
- 未传 `dataset_ids` 时使用：

```text
["dataset_default_equipment_ops"]
```

- 对 `dataset_ids` 做去空、去重和数量上限校验；第一版最多 10 个。
- API 初始化 `QueryGraphState` 时写入：

```text
owner_user_id
tenant_id = "tenant_default"
dataset_ids
```

- 聊天页复用导入页相同的 `kb_user_id` localStorage key，所有 `/query` 请求带 `X-User-Id`。
- 阶段 5 不改造 `/history/{session_id}` 的完整用户隔离模型，但要在文档中明确这是阶段 7 的剩余边界；不得误写成已经完成聊天历史权限隔离。

验收重点：

- 用户 A、用户 B 发起相同 query 时，state 中 owner 不同。
- 缺失 header 的查询不会进入 LangGraph。
- 空 dataset 列表、重复 dataset 和超长列表有确定行为。

实现状态：已完成。

- 查询和导入 API 已复用同一个轻量 `X-User-Id` 解析函数；缺失或空白 header 返回 400，并且发生在创建 SSE 队列和调度查询图之前。
- `QueryRequestParam.dataset_ids` 已实现默认值、去空、保序去重和最多 10 个不同 dataset 的校验；显式空列表或全空白列表返回 422。
- `owner_user_id`、`tenant_id`、`dataset_ids` 已写入 `QueryGraphState`，同步和流式查询都显式传递相同上下文。
- 聊天页已复用 `kb_user_id` 并在 `/query` 请求中发送 `X-User-Id`。
- 本部分只固化查询输入上下文；Milvus visibility/owner/dataset 权限过滤仍属于后续任务，聊天历史的完整用户隔离仍属于阶段 7。

### 2. 新增 Planner、Action、Observation 和引用契约

目标：先固定稳定的业务协议，再改图和节点。

建议新增：

```text
app/rag/query/contracts.py
app/rag/query/planner.py
```

`contracts.py` 至少定义：

```python
class QueryAction(str, Enum):
    LOCAL_SEARCH = "local_search"
    HYDE_SEARCH = "hyde_search"
    WEB_SEARCH = "web_search"
    ANSWER = "answer"
    ASK_CLARIFICATION = "ask_clarification"
    REFUSE = "refuse"


class IdentifierResolutionStatus(str, Enum):
    NOT_APPLICABLE = "not_applicable"
    EXACT_MATCH = "exact_match"
    FALLBACK_EXACT_MATCH = "fallback_exact_match"
    SUGGESTION_REQUIRED = "suggestion_required"
    NOT_FOUND = "not_found"


class PlannerDecision(BaseModel):
    action: QueryAction
    query: str
    reason_code: PlannerReasonCode


class EvidenceSummary(BaseModel):
    document_id: str | None
    chunk_id: str | int | None
    title: str
    source_type: EvidenceSourceType
    rerank_score: float | None
    matched_identifiers: dict[str, list[str]]
    content_excerpt: str


class RetrievalObservation(BaseModel):
    action: QueryAction
    status: ObservationStatus
    channel_counts: dict[str, int]
    candidate_count: int
    reranked_count: int
    top_rerank_score: float | None
    requested_identifiers: dict[str, list[str]]
    matched_identifiers: dict[str, list[str]]
    identifier_resolution_status: IdentifierResolutionStatus
    suggested_identifiers: dict[str, list[str]]
    citation_count: int
    evidence_summaries: list[EvidenceSummary]
    evidence_ambiguous: bool
    clarification_question: str | None
    duration_ms: int
    error_code: str | None
    used_structured_filter: bool
    filter_fallback: bool


class PlannerHistoryItem(BaseModel):
    step: int
    decision: PlannerDecision
    execution_status: PlannerExecutionStatus


class PlannerContext(BaseModel):
    original_query: str
    current_query: str
    subject_resolution_status: SubjectResolutionStatus
    subject_ids: list[str]
    subject_candidates: list[str]
    clarification_question: str | None
    query_identifiers: dict[str, list[str]]
    latest_observation: RetrievalObservation | None
    action_history: list[PlannerHistoryItem]
    planner_step: int
    max_steps: int
    allowed_actions: list[QueryAction]


class Citation(BaseModel):
    document_id: str | None
    chunk_id: str | int | None
    title: str
    source: str
    score: float
    source_type: EvidenceSourceType
```

`EvidenceSummary.content_excerpt` 只用于当前 Planner 决策的受限证据上下文，必须限制 top-N、单条长度和总长度。Trace 持久化层不得默认保存该正文片段，只保存可重放的证据标识与摘要 hash。

阶段 5 的 `evidence_ambiguous` 由可重放的结构化规则产生：例如候选证据属于多个互不兼容的设备型号/版本，且用户问题缺少可区分信息。低分或候选数量多不能单独判定为歧义；`clarification_question` 也必须由命中的差异字段确定性生成。

标识字段必须区分三种数据所有权：`PlannerContext.query_identifiers` 保存用户问题中实际出现并归一化的编号，不能被系统猜测值覆盖；`RetrievalObservation.requested_identifiers` 保存本次 Action 实际使用的用户编号快照，供 Observation 自身校验和 Trace 重放；`matched_identifiers` 保存证据中实际出现的编号；`suggested_identifiers` 只保存等待用户确认的纠错候选。向量相似度和 rerank 分数不能把 `E020` 静默改写成 `E021`。

`PlannerDecision.reason_code` 第一版使用固定枚举或集中常量，至少覆盖：

```text
SUBJECT_AMBIGUOUS
SUBJECT_REQUIRED
SUBJECT_NOT_FOUND
IDENTIFIER_CONFIRMATION_REQUIRED
IDENTIFIER_NOT_FOUND
REALTIME_QUERY
INITIAL_LOCAL_SEARCH
LOCAL_EVIDENCE_SUFFICIENT
LOCAL_EMPTY
LOCAL_LOW_SCORE
HYDE_EVIDENCE_SUFFICIENT
HYDE_STILL_INSUFFICIENT
WEB_EVIDENCE_AVAILABLE
WEB_EMPTY_OR_FAILED
EVIDENCE_AMBIGUOUS
ACTION_EXECUTION_ERROR
SAFE_GUARD_TRIGGERED
```

`QueryPlanner` 接口：

```python
class QueryPlanner(Protocol):
    policy_version: str

    def plan(self, context: PlannerContext) -> PlannerDecision:
        ...
```

阶段 5 线上策略第一版只计划实现：

```text
RuleBasedPlanner(policy_version="rule-v1")
```

验收重点：

- 所有 Planner 输出都能通过 Pydantic/Enum 校验。
- reason code 是机器可读枚举，不写自由文本分析。
- Planner 不依赖 Milvus、Mongo、LLM 或 HTTP。

实现状态：契约层已完成。

- 已新增 `contracts.py`，用 Python `str, Enum` 固化 `QueryAction`、`PlannerReasonCode`、主体确认状态、编号确认状态、Observation 状态、执行状态和证据来源类型。
- 已新增 `PlannerDecision`、`EvidenceSummary`、`RetrievalObservation`、`PlannerHistoryItem`、`PlannerContext` 和 `Citation` Pydantic 契约；所有模型禁止额外字段并对关键字段关系做一致性校验。讨论编号纠错边界后，Observation 已补充 `identifier_resolution_status` 和 `suggested_identifiers`。
- `EvidenceSummary` 已限制单条 500 字符、最多 5 条、总计 2000 字符；正文片段只属于运行时 Planner Context，后续 Trace 不得默认持久化。
- 已新增无外部 I/O 的 `QueryPlanner` Protocol 和稳定策略版本常量 `rule-v1`。
- 本部分只完成协议和接口。具体 `RuleBasedPlanner` 决策函数仍按任务 8 实现，尚未接入 LangGraph；`QueryGraphState` 扩展属于任务 3，Trace 持久化和 citations 输出也尚未接入。
- 当前契约专项测试 `34 passed`；编号确认契约回改后的全量回归 `180 passed`。

### 3. 扩展 QueryGraphState，并保持节点 partial state

目标：让每个跨节点字段的来源、用途和持久化边界清晰。

`QueryGraphState` 新增或明确：

```text
# 请求和权限
owner_user_id: str
tenant_id: str
dataset_ids: list[str]

# 主体确认和设备标识
subject_resolution_status: SubjectResolutionStatus | None
subject_candidates: list[str]
clarification_question: str | None
query_identifiers: dict[str, list[str]]

# Planner
trace_id: str
planner_step: int
policy_version: str
current_planner_decision: PlannerDecision | None
planner_action_history: list[PlannerHistoryItem]
planner_type: str
planner_runtime_metadata: dict[str, object]

# Observation 和检索配置
retrieval_observation: RetrievalObservation | None
retrieval_mode: str
retrieval_config_version: str

# 召回和输出
retrieval_channel_results: dict[str, list[dict]]
citations: list[Citation]
terminal_reason_code: PlannerReasonCode | None
answer_runtime_metadata: dict[str, object]
query_started_at: str
```

为保证阶段 5 分步实施期间当前查询图仍可运行，任务 3 暂时保留下面这些仍被现有节点实际读写的运行时字段：

- `embedding_chunks`：如果最终继续保留，可改名或映射为 original-query 本地候选。
- `hyde_embedding_chunks`：HyDE Action 的本地候选。
- `web_search_docs`：Web Action 候选。
- `rrf_chunks`：跨检索批次去重融合后的本地候选。
- `reranked_docs`：统一的最终证据候选，必须保留完整来源元数据。

这里是查询代码和 State 契约分步切换时的临时过渡，不是 Milvus 历史数据或 schema 迁移。新 Planner 主链路及相关检索节点完成切换后，应一次性完成字段重命名、归属收口或删除；不实现新旧 State 双写、长期适配层或旧字段兼容分支。

Milvus 侧继续沿用本阶段已经确认的策略：当前只有少量测试数据，schema 调整时直接删除受影响的 chunk collection、按最终 schema 重建并重新导入测试文档，不做历史 backfill、双写迁移或在线兼容。

所有节点继续返回 partial state，不把整个 state 原样返回。默认 state 中的 list/dict 必须深拷贝，避免查询之间互相污染。

验收重点：

- `tests/test_state_defaults.py` 精确覆盖新字段集合和默认类型。
- 两次 `create_query_default_state` 的 history/action/citation/dict 字段互不共享。
- 查询节点只返回自己负责的字段；即使底层 service 返回完整字典，也不会把整个旧 State 原样交回 LangGraph。

实现状态：已完成。

- `QueryGraphState` 已按请求范围、主体确认、Planner、Observation、召回通道、引用和运行元数据分组扩展；每个字段都补充了中文含义、来源、用途、默认值和持久化边界。
- Planner/Observation/Citation 字段直接复用第二部分的 Pydantic/Enum 契约，不继续使用含义不明确的裸 `dict` 或自由字符串。
- 查询入口为每次真实执行生成独立 UUID `trace_id`，并写入带 UTC 时区的 ISO 8601 `query_started_at`；同一 `session_id` 的多次查询也不会复用 Trace。
- 当前主图尚未运行 Planner，所以 `planner_step` 保持 `0`，`policy_version`、`planner_type` 保持空字符串，Decision 和 Observation 保持 `None`；不提前伪装成已经执行 `rule-v1`。
- 查询日志优先使用 `trace_id` 关联单次执行；在旧调用方没有该字段时，才依次回退到 `task_id` 或 `session_id`。
- 当前仍被固定查询图读写的五个旧召回字段继续保留，但只作为运行时代码分步切换边界，不涉及 Milvus 数据迁移，也不新增双写适配层。
- 所有七个查询节点均保持 partial state：主体确认、普通检索、HyDE、Web、RRF、rerank 和答案节点只返回各自负责的字段。
- 默认 State、State 复制和查询入口专项测试已覆盖字段全集、可变容器深拷贝、Trace 唯一性、UTC 时间、日志 Trace 优先级及 partial state 精确字段集合；定向回归 `52 passed`，全量回归 `155 passed`。

### 4. 统一构建权限、主体和结构化标识过滤表达式

目标：所有本地召回通道使用同一份、可测试的 Milvus filter，不允许普通检索和 HyDE 各自拼接。

建议在 `chunk_retrieval_utils.py` 中新增：

```text
build_chunk_access_filter(...)
build_structured_identifier_filter(...)
build_chunk_retrieval_filter(...)
```

基础过滤逻辑：

```text
dataset_id in 当前 dataset_ids
AND subject_id in 已确认 subject_ids
AND enabled == true
AND (
    visibility == "public"
    OR (visibility == "shared" AND tenant_id == 当前 tenant_id)
    OR owner_user_id == 当前 user_id
)
```

要求：

- 所有字符串、列表都走统一 Milvus 转义函数。
- dataset、subject 为空时不能生成 `in []` 后静默全库搜索；调用方必须得到明确校验错误或 Planner Observation。
- 权限条件必须加完整括号，避免 `AND/OR` 优先级导致私有数据越权。
- 普通检索、HyDE、BM25、learned sparse 和 dense 请求必须复用完全相同的 expr。
- `enabled == true` 从阶段 5 起强制生效，即使阶段 6 尚无人工禁用入口。

用户隔离测试矩阵：

| 数据 | user_a | user_b |
|---|---:|---:|
| user_a private | 可召回 | 不可召回 |
| user_b private | 不可召回 | 可召回 |
| public | 可召回 | 可召回 |
| tenant_default shared | 可召回 | 可召回 |
| enabled=false | 不可召回 | 不可召回 |
| 其他 dataset | 不可召回 | 不可召回 |

实现状态：已完成。

- 已在 `chunk_retrieval_utils.py` 新增 `build_chunk_access_filter`、`build_structured_identifier_filter`、`build_chunk_retrieval_filter` 和 State 适配入口，所有本地召回统一从 `QueryGraphState` 读取过滤上下文。
- access filter 已强制包含本次 `dataset_ids`、`enabled == true` 和完整括号包裹的 public/shared/owner 可见性条件；`shared` 只允许当前 `tenant_id`，私有数据只允许当前 `owner_user_id`。
- `dataset_ids`、`subject_ids`、owner、tenant 任何必填范围为空都会在向量化和 Milvus 调用前抛出明确错误，不生成 `in []`，也不删除条件后回退全库搜索。
- 所有动态字符串和字符串列表统一经过 Milvus 转义、去空、保序去重；误把单个字符串当列表传入会直接失败，避免按字符拆分出错误表达式。
- 结构化标识字段使用固定白名单，只支持当前 chunk schema 已存在的 `equipment_model`、`alarm_code`、`part_name`、`sop_type`、`safety_level`、`maintenance_stage`；未知字段不会被拼进 expr。第五部分现已提供自动提取输入；`sop_code/part_number` 因 schema 尚无独立字段，只进入增强检索文本，不进入 expr。
- 普通本地检索与 HyDE 已改为复用完全相同的最终 expr；当时 hybrid search 内的 dense 和 learned sparse 两个 `AnnSearchRequest` 也携带同一表达式。第七部分接入的 BM25 已继续复用该构建器，没有新增专用权限表达式。
- 权限分组、用户差异、动态字符串转义、空范围拒绝、结构化字段白名单、普通/HyDE 一致性及 dense/learned sparse 一致性已加入专项测试；定向回归 `41 passed`，全量回归 `172 passed`。

### 5. 增加设备运维标识提取、归一化与两段式检索

目标：让设备型号、报警码、SOP 编号和零件编号不再只依赖 embedding 猜测。

建议新增：

```text
app/rag/query/query_identifier_service.py
```

第一版使用确定性规则，不引入额外 LLM：

- `equipment_model`：识别字母/数字组合及空格、连字符变体，例如 `HAK180`、`HAK-180`、`HAK 180`，归一为同一 canonical form。
- `alarm_code`：识别 `E021`、`E020` 等明确报警/故障码，统一大写和分隔符。
- `sop_code`：识别包含 `SOP` 前缀的编号；当前 chunk 没有独立 `sop_code` 字段时，先进入 `lexical_text` 精确词召回，不伪装成 `sop_type`。
- `part_number`：识别明确零件/备件编号；当前 chunk 没有独立 `part_number` 字段时，先进入 `lexical_text`，评测确认价值后再决定是否扩 schema。
- 中文部件名继续使用现有 `part_name`，同时参与 lexical text。

两段式检索：

```text
第一段：基础权限/subject filter + 可映射的精确结构化字段 filter
  -> 命中用户输入的同一编号：作为答案证据保留并显著优先
  -> 0 结果：记录 filter_fallback=true
第二段：只保留基础权限/subject filter，使用原始 query + normalized identifiers 做多通道召回
  -> 正文/lexical_text 仍命中用户输入的同一编号：
       identifier_resolution_status=fallback_exact_match
       说明可能只是导入 metadata 漏填，可以进入 rerank 和答案证据
  -> 只找到不同但相近的编号：
       identifier_resolution_status=suggestion_required
       requested_identifiers 保持用户原编号
       写入 suggested_identifiers 和 clarification_question
       只能作为纠错候选，citation_count=0，不得进入 answer
  -> 没有相同编号或可靠候选：
       identifier_resolution_status=not_found
       请用户核对设备屏幕、报警码或上传图片
```

编号候选优先从当前权限、dataset 和已确认 subject 范围内的合法编号词典/索引产生，再使用编辑距离、编号格式和文档频率排序；embedding 只辅助结合自然语言上下文排序候选，不能单独证明两个编号相同。

这样既能提高精确编号命中率，也避免导入侧结构化字段缺失时直接把正确文档过滤掉。第二段放宽的只有结构化编号条件，权限、dataset、subject 和 enabled 条件始终保留；放宽召回得到的“不同编号”只用于询问用户，不是最终答案证据。

验收重点：

- `HAK180`、`HAK-180`、`HAK 180` 归一结果一致。
- `E021/E020` 能分别提取，不能合并为一个错误编号。
- 精确 filter 0 命中时会降级召回，并在 Observation/Trace 中可见。
- 用户输入 `E020`、当前设备只找到 `E021` 时必须输出 `suggestion_required` 和追问，不能进入 answer，Citation 必须为空。
- metadata 缺失但宽松召回正文仍明确包含 `E020` 时输出 `fallback_exact_match`，允许继续作为同编号证据。
- 多个相近候选、跨设备候选和无权限候选都不能被静默选为纠正结果。
- 每个标识提取规则都有正例、变体和误匹配反例测试。

实现状态：已完成。

- 已新增 `query_identifier_service.py`，查询在进入 LangGraph 前从 `original_query` 提取标识；`HAK180`、`HAK-180`、`HAK 180` 统一为 `HAK 180`，报警码分别保留，SOP/零件编号采用带明确前缀或上下文的保守规则。
- `sop_code` 和 `part_number` 已进入查询标识契约和增强检索文本，但当前 Milvus 没有对应独立结构化字段，因此不会生成虚假的 schema filter；第七部分已通过 `lexical_text`/BM25 提供词法召回，是否增加独立字段仍由评测决定。
- 普通本地检索现已复用一次 dense/learned sparse 向量执行两段搜索。第一段使用可映射结构化标识；未完整覆盖用户编号时，第二段只移除结构化标识条件，dataset、subject、enabled、public/shared/owner 条件完全保留。
- 第二段正文仍明确出现用户同一编号时输出 `fallback_exact_match`；只找到编辑距离为 1 且格式、同设备上下文兼容的不同编号时输出 `suggestion_required`；没有可靠候选时输出 `not_found`。
- 纠错候选通过带完整基础 filter 的 Milvus metadata 查询和宽松召回 chunk 产生，按编辑距离、文档频次和稳定字典序排序。embedding 结果不能单独证明两个编号相同。
- 当前 Planner 路由尚未接入，因此 RRF、rerank 和答案出口增加了临时兼容保护：`suggestion_required/not_found` 不进入 reranker 或答案 LLM，只交付确定性追问，并显式保持 `citations=[]`。任务 9 接入 Planner 后由 `ask_clarification` 路由更早终止，出口保护继续作为最后防线。
- 普通本地检索已经把编号结果写入强类型 `RetrievalObservation`；Trace 持久化属于后续任务，本部分没有误写成已经完成可查询 Trace。
- HyDE 当前继续复用相同的安全过滤表达式，但不单独写第二份 Observation，避免并行节点同时覆盖同一个 State 字段；本轮编号确认以普通本地检索的两段结果为准。
- 第五部分新增 15 个专项用例；完成实现后的全量回归为 `196 passed`。

### 6. 重构本地召回通道与 RRF 融合

目标：支持可切换召回组合，并区分“单次本地 Action 内通道融合”和“所有已执行检索 Action 的累计结果融合”。

Planner 与融合的执行时序必须是闭环，而不是查询开始时一次性决定并并发运行全部检索：

```text
初始 Planner：读取 query / subject / realtime 规则
  -> 明显实时：可直接 web_search
  -> 普通知识库问题：默认 local_search

每次检索 Action 完成：
  -> 保存该 Action 的原始排名列表
  -> 从所有已执行 Action 的原始列表重算一次跨 Action RRF
  -> 对累计候选统一 rerank
  -> 生成结构化 Observation
  -> Planner 根据 Observation 决定 answer / HyDE / Web / 追问 / 拒答
```

因此 local 证据充分时不会执行 HyDE/Web；local 有候选但证据质量不足时才执行 HyDE；local 为空时可以按规则直接 Web，HyDE 执行后累计证据仍不足时也可以继续 Web。实时问题是例外，可以由初始 Planner 直接 Web，或者按规则选择 local + Web 获取“本地背景 + 最新信息”。

召回模式固定为：

```text
dense_learned_sparse
dense_bm25
dense_learned_sparse_bm25
```

阶段 5A 中：

- `MilvusGateway.create_requests` 改为按 `retrieval_mode` 创建 2 或 3 个 `AnnSearchRequest`。
- dense 请求输入 dense vector。
- learned sparse 请求输入 BGE-M3 sparse vector。
- BM25 请求输入原始文本 query，由 Milvus Function 使用与入库相同的 Analyzer 生成查询稀疏表示。
- 所有请求使用同一个 expr 和同一个 per-channel top-k。
- Milvus 内部多通道结果使用 `RRFRanker(k=60)`，不直接相加三种原始分数。
- 现有 `WeightedRanker` 仍可供标准主题/别名 collection 使用，不要为改 chunk 检索破坏阶段 2；chunk 检索显式选择 RRF。

跨 Action 融合：

- 融合输入不是固定的 original/HyDE 两路，而是 Action history 中所有已经实际执行的检索 Action 原始排名列表：`original local_search`、可选 `hyde_search`、可选 `web_search`。
- Planner 未执行某个 Action 时，不创建对应伪列表，也不让该 Action 参与融合。合法组合至少覆盖：仅 original、original + HyDE、original + Web、original + HyDE + Web，以及明显实时问题仅 Web。
- 使用通用 `fuse_ranked_candidate_lists` 对当前全部原始 Action 列表一次性重算外层 RRF。禁止先把 original + HyDE 融成一份，再把这份 RRF 结果与 Web 继续套一层 RRF；嵌套做法会丢失原始 Action 名次，并让最终权重依赖执行顺序。
- 本地候选按 `chunk_id` 去重；Web 候选按规范化 URL 去重。Web 的 `document_id/chunk_id/index_version` 必须为 `None`，不能为了复用本地去重逻辑伪造 ID。
- 单路、空路都返回结构化结果，不因某一路为空抛异常；只有实际执行过但返回空结果的 Action 才在 Observation 中记录空结果。
- 外层 RRF 只负责消除各 Action 原始分数量级差异并形成候选顺序；随后必须让本地候选和 Web 候选一起使用原始用户问题做统一 rerank，最终相关性不能只看 RRF 名次。
- 排名并列时，本地使用稳定的 `chunk_id`、Web 使用规范化 URL 作为次级排序键，保证固定输入下顺序稳定。

跨 Action 累计示例：

```text
local 证据充分：
  [original]

local 不足、HyDE 后充分：
  [original, hyde]

local 和 HyDE 后仍不足：
  [original, hyde, web]

明显实时问题：
  [web] 或 [original, web]
```

改造当前格式转换：

- 定义统一 `RetrievalCandidate` 形态。
- rerank 前后必须保留：

```text
document_id
chunk_id
dataset_id
index_version
chunk_index
title
source_title
content/text
source_type
retrieval_channels
retrieval_rank
retrieval_score
rerank_score
url
```

- Web 候选没有本地 ID 时，`document_id/chunk_id/index_version` 为 `None`，不能伪造本地标识。

验收重点：

- 三种模式生成正确数量和字段的 `AnnSearchRequest`。
- 三路原始分数量级不同不会被直接相加。
- local 或 HyDE 任一路为空仍能继续。
- Web 被 Planner 执行后会进入跨 Action 累计候选和统一 rerank；未执行时不出现 Web 伪列表。
- original/HyDE/Web 的外层 RRF 从各 Action 原始排名一次性重算，结果不依赖 Action 到达顺序，也不存在 RRF 套 RRF。
- 仅 original、original + HyDE、original + Web、original + HyDE + Web、仅 Web 五类组合都有确定性测试。
- rerank 后本地候选仍保留 `document_id/chunk_id`。

实现状态：已完成。

- 已新增 `RetrievalMode` 三种关闭枚举和 `RetrievalChannel` 六种通道/Action 来源枚举；当前默认仍是 `dense_learned_sparse`，未知模式在创建 Milvus 请求前明确失败。
- `create_hybrid_search_requests` 已能按模式创建 dense + learned sparse、dense + BM25 或三路 `AnnSearchRequest`；所有请求复用同一个 expr 和 per-channel top-k。包含 BM25 的模式必须提供原始文本 query；第七部分已补齐 schema/Function，但默认模式仍等待评测结论后再决定是否切换。
- chunk hybrid search 已显式使用 `RRFRanker(k=60)`；标准主题和别名 collection 继续保留 `WeightedRanker`，没有因 chunk 改造破坏阶段 2。
- 已新增强校验 `RetrievalCandidate` 契约。普通本地、HyDE、Web 从产生候选起就统一携带来源类型、模式通道、Action 来源、排名、召回分和 rerank 分；本地必须保留 document/chunk/dataset/index 身份，Web 必须保留真实 URL 且所有本地身份为 `None`。Milvus 只返回内部 RRF 后列表，因此本地模式通道不冒充逐通道命中明细。
- 已新增 `fuse_ranked_candidate_lists`。它从 original、HyDE、Web 当前实际存在的原始排名列表一次性计算外层 RRF；本地按 `chunk_id`、Web 按规范化 URL 去重，重复候选合并 `retrieval_channels`，并用稳定身份键处理并列。
- rerank 不再把本地/Web 压缩成只有 `title/text/type/score` 的临时字典，也不再在 RRF 后重复追加 Web；现在直接对累计 Candidate 统一打分，并完整保留 document/chunk/dataset/index、召回来源和 URL。
- Web-only 候选可以在不伪造本地标准主题的情况下进入答案前置校验，为后续初始 Planner 直接 Web 留出正确边界。
- 当前 LangGraph 仍是旧的并发执行图，尚未根据 Action history 真正决定“哪些 Action 被执行”；任务 9 接入闭环 Planner 后，未执行 Action 的字段保持空列表即可直接复用本轮外层融合。该边界没有误写成 Planner 路由已经完成。
- 第六部分新增 17 个专项用例；完成实现后的全量回归为 `213 passed`。

### 7. 增加 BM25 实验 schema 与 lexical_text

目标：在相同 chunk 上公平比较 learned sparse 与 Milvus BM25。

阶段 5A 临时 schema 调整：

- 将现有 `sparse_vector` 在 chunk collection 中明确改名为：

```text
learned_sparse_vector
```

- 新增：

```text
lexical_text: VARCHAR
bm25_sparse_vector: SPARSE_FLOAT_VECTOR
```

- `lexical_text` 开启 Analyzer。
- 在 schema 上新增 BM25 Function：

```text
input: lexical_text
output: bm25_sparse_vector
```

- 应用只写 `lexical_text`，不自行计算或写入 `bm25_sparse_vector`。
- `bm25_sparse_vector` 使用适配 BM25 的稀疏倒排索引和 metric。

`lexical_text` 由以下内容按固定顺序、去空后拼接：

```text
standard_subject_name
equipment_model
alarm_code
part_name
sop_type
safety_level
maintenance_stage
source_title
title
parent_title
content
```

同时追加标识归一化变体，例如：

```text
HAK180 HAK-180 HAK 180
E021 E-021 E 021
```

Analyzer 验证先于全量重建：

- 使用 Milvus Analyzer 测试接口验证真实 token，而不是只看配置。
- 至少覆盖中文说明句、中英文混合、连字符、空格、大小写、设备型号、报警码、SOP/零件编号。
- Analyzer 无法稳定保留精确编号时，优先通过 lexical_text 追加规范化变体和结构化 filter 解决，不盲目堆叠复杂 tokenizer。

阶段 5B schema 收口：

- 如果保留 learned sparse，则最终字段名使用 `learned_sparse_vector`，同步所有导入、检索和数据字典代码。
- 如果 learned sparse 被淘汰，删除其生成、字段、索引和请求代码，避免 BGE-M3 sparse 继续产生无用成本。
- 如果 BM25 被淘汰，删除 `lexical_text`、BM25 Function、`bm25_sparse_vector` 和相关分支。
- 如果保留三路，则三路都进入版本化 retrieval config，并继续使用 RRF。

实现状态：阶段 5A 实验 schema 已完成；阶段 5B 最终收口待固定评测集结论。

当前实现说明：

- chunk collection 的 BGE-M3 字段已从 `sparse_vector` 收口为 `learned_sparse_vector`；标准主题和别名 collection 仍保留各自的 `sparse_vector`，没有做无关的全库机械改名。
- 新增 `lexical_text`、`bm25_sparse_vector` 和 `chunk_lexical_text_bm25` Function；learned sparse 使用 `IP`，BM25 使用 `BM25` metric，两者各自建立稀疏倒排索引。
- `lexical_text` 使用 `jieba + cnalphanumonly + lowercase` Analyzer。创建空 collection 前会调用真实 Milvus `run_analyzer`，要求至少保留 `hak180`、`e021` 和中文 token；校验失败会中止建库。
- 入库按文档中约定的固定字段顺序构造 `lexical_text`，并追加同一编号的紧凑、连字符和空格变体。E020/E021 始终是两个独立编号，变体生成不承担纠错。
- BGE-M3 节点只写 `dense_vector` 和 `learned_sparse_vector`；应用侧写入 `lexical_text`，禁止写入 Function 输出 `bm25_sparse_vector`。发现旧 `sparse_vector` 输入会立即失败。
- 发现已有 chunk collection 缺少新字段时，不做历史 backfill、双写或隐式兼容，而是明确要求删除测试 collection 后重新导入。
- 三种 `RetrievalMode` 现在都具备物理 schema 和请求能力，但默认仍保持 `dense_learned_sparse`；在固定评测集得出结果前，不把 BM25 实验模式误写成线上最优方案。

验证结果：

- 当前 Milvus Server 已确认是 `2.6.15`；PyMilvus `2.6.10` 真实 `CollectionSchema` 已验证 Function input/output 和两个稀疏索引参数可构造。
- 已连接的 Milvus Analyzer 实测返回 `hak180`、`e021` 和中文 token。
- 第七部分新增 12 个专项测试；完成本部分后的全量回归为 `225 passed`。

### 8. 实现确定性的 RuleBasedPlanner

目标：形成线上默认策略、阶段 8 baseline 和阶段 9 故障回退。

第一版规则顺序：

1. 主体存在候选歧义，或问题/历史中完全没有可用主体：`ask_clarification`。
2. 用户明确提到了主体但无法映射到知识库，且不是明显实时问题：`refuse`。
3. 问题明显要求实时信息：`web_search`。
4. 已确认本地主题且还未检索：`local_search`。
5. 最新 Observation 为 `suggestion_required`：使用 `IDENTIFIER_CONFIRMATION_REQUIRED` 追问用户，禁止 answer。
6. 最新 Observation 为 `not_found`：使用 `IDENTIFIER_NOT_FOUND` 请用户核对编号，禁止 answer。
7. 最新 Observation 显示其他可由用户补充信息解决的证据歧义：`ask_clarification`。
8. local rerank 证据充分，且编号为 `exact_match`、`fallback_exact_match` 或不适用：`answer`。
9. local 有候选但证据不足，且未执行 HyDE：`hyde_search`。
10. local 为空，或 HyDE 后仍不足，且允许 Web：`web_search`。
11. Web 有可用候选且没有编号不一致：`answer`。
12. Web 为空/失败或问题不适合 Web：`refuse`。
13. 任意安全约束触发：`refuse`。

“证据充分”不能凭主观固定一个阈值：

- 先用阶段 5 专项评测集采集 reranker score 分布。
- 标注哪些 query 的候选足以回答、哪些应该追问/拒答。
- 在开发集上确定 `RERANK_EVIDENCE_THRESHOLD`，再冻结到 `retrieval_config_version`。
- held-out 小测试集只做验收，不用于反复调阈值。

实时性判断使用版本化关键词/正则规则；命中规则、规则版本和结果写入 Trace。

Planner 伪代码：

```text
plan(context):
    validate_step_and_transition()

    if subject_status in (ambiguous, no_mention):
        return ask_clarification
    if subject_status == not_found and not realtime:
        return refuse
    if realtime and web_search not executed:
        return web_search
    if local_search not executed:
        return local_search
    if latest_observation.identifier_resolution_status == suggestion_required:
        return ask_clarification(identifier_confirmation_required)
    if latest_observation.identifier_resolution_status == not_found:
        return ask_clarification(identifier_not_found)
    if latest_observation.evidence_ambiguous:
        return ask_clarification
    if evidence_is_sufficient and identifier_is_confirmed_or_not_applicable:
        return answer
    if local_has_candidates and hyde_search not executed:
        return hyde_search
    if web_search not executed and web_is_allowed:
        return web_search
    return refuse
```

验收重点：

- RuleBasedPlanner 是无 I/O 的纯函数。
- 给定固定 Observation 序列能稳定生成完整 Action Trace。
- 不会出现 `local -> hyde -> web -> hyde` 循环。
- 最大步骤和非法转移都会安全结束。

实现状态：已完成 RuleBasedPlanner 纯函数；LangGraph 接线仍属于第 9 部分。

当前实现说明：

- 新增 `RuleBasedPlannerConfig`，要求显式传入 `rerank_evidence_threshold` 和 `retrieval_config_version`。代码不提供主观默认阈值，后续必须用设备运维开发集确定后注入。
- `RuleBasedPlanner.plan(context)` 只读取 `PlannerContext` 并返回 `PlannerDecision`，不访问 Milvus、Mongo、HTTP、LLM，也不修改 Context；相同输入和配置稳定返回相同 Action/`reason_code`。
- `PlannerContext` 新增 `web_search_allowed` 和 `safe_guard_triggered`。前者限制本次查询能否联网，后者一旦为 true 必须优先拒答；`allowed_actions` 现在强制包含 `refuse` 安全出口。
- 实时问题识别使用 `realtime-keywords-v1` 保守正则。它识别“今天/最新 + 公告/召回/发布”等明确实时组合和显式联网请求，不会仅因“当前报警”或“最新维修方法”就绕过本地知识库。
- 主体歧义/未提及优先追问；主体未找到的非实时问题拒答；明确实时问题可直接 Web；普通已确认主体从 local 开始。
- `suggestion_required/not_found/evidence_ambiguous` 的优先级高于 rerank 分数。即使相近编号候选分数很高，也只能追问，不能 answer。
- 证据充分同时要求 Action 成功、有候选、有 rerank 结果、分数达到配置阈值，并且编号状态为 `not_applicable/exact_match/fallback_exact_match`。
- local 低分且有候选时只执行一次 HyDE；local 为空可直接 Web；HyDE 仍不足才 Web；Web 有充分证据才 answer，否则 refuse。本地或 HyDE 执行异常可降级 Web，Web 自身失败只能拒答。
- Action history 强制单向转移并禁止重复检索。步骤断裂、Observation 与最后 Action 不一致、`local -> Web -> HyDE` 回退、终止后继续规划、最大步骤耗尽或目标 Action 不在白名单，都会使用 `SAFE_GUARD_TRIGGERED` 收口到 refuse。
- 当前固定 LangGraph 尚未实例化或调用该 Planner，`QueryGraphState.policy_version/planner_step/action_history` 仍保持现有未接入语义；第 9 部分负责节点接线和状态写入。

验证结果：

- 第八部分新增 37 个 RuleBasedPlanner 专项测试；连同既有 Planner 契约测试共 `72 passed`。
- 完成本部分后的全量回归为 `263 passed`。

### 9. 改造主体确认、LangGraph 路由和异常收口

目标：让图真正由 PlannerDecision 驱动，而不是用节点内部隐式状态决定流程。

主体确认改造：

- `confirm_subject_name` 不再把候选追问或“未找到”直接写入最终 `answer`。
- 输出：

```text
subject_resolution_status = confirmed | ambiguous | not_found | no_mention
subject_candidates
clarification_question
subject_ids
standard_subject_names
rewritten_query
```

- 用户消息保存历史的现有时机要补测试，避免 confirmed 分支提前 return 后漏存用户问题。

图节点建议新增：

```text
node_query_planner.py
node_retrieval_observation.py
node_terminal_response.py
node_trace_finalize.py
```

路由函数只读取已经校验的 `current_planner_decision.action`，不重复实现业务规则。

异常处理：

- Action 节点捕获可预期的外部调用错误，写标准 `error_code` 和耗时到 Observation。
- Planner 根据错误类型决定 fallback 或拒答。
- 编程错误仍由查询入口记录 Trace failed 并返回 500，不能全部吞掉伪装成“证据不足”。
- Trace/可观测写入失败不得泄漏其他用户数据，也不得改变检索权限表达式。

### 10. 建立结构化 Retrieval Trace

目标：每次查询都能回答“输入是什么、做了什么、为什么、用了哪些证据、版本是什么”。

建议新增：

```text
app/infra/persistence/retrieval_trace_repository.py
```

Mongo collection：

```text
retrieval_traces
```

Trace 顶层字段：

```text
trace_id
session_id
owner_user_id
tenant_id
dataset_ids
original_query
rewritten_query
subject_ids
standard_subject_names
query_identifiers
identifier_resolution_status
suggested_identifiers
policy_version
planner_type
provider
model_id
model_revision
prompt_version
planner_usage
answer_provider
answer_model_id
answer_model_revision
answer_prompt_version
answer_usage
retrieval_config_version
retrieval_mode
index_versions
status
terminal_action
terminal_reason_code
planner_steps
channel_hits
final_citations
started_at
completed_at
total_duration_ms
error_code
```

`planner_steps[]` 每步至少保存：

```text
step
input_observation
decision.action
decision.query
decision.reason_code
execution_status
output_observation
duration_ms
planner_usage
```

`planner_usage` 和 `answer_usage` 使用相同的结构，分开统计 Planner 决策与最终答案生成的开销：

```text
input_tokens
output_tokens
total_tokens
duration_ms
estimated_cost
currency
```

阶段 5 使用规则 Planner 时，`planner_type="rule"`，`provider/model_id/model_revision/prompt_version` 为 `null`，Planner token 数和估算成本为 `0`。这些字段在本阶段先固化数据契约，阶段 8-9 的 API、本地零样本、SFT 和 GRPO Planner 复用同一 Trace schema。

`channel_hits` 只保存可核验和重放所需的结构化信息：

```text
channel
document_id
chunk_id
index_version
rank
retrieval_score
rerank_score
matched_identifiers
content_excerpt_hash
```

`planner_steps[].input_observation/output_observation` 是运行时 Observation 的持久化投影：写入前必须移除 `EvidenceSummary.content_excerpt`，只保留证据 ID、标题、分数、`requested_identifiers`、命中标识、`identifier_resolution_status`、`suggested_identifiers`、追问文本和 `content_excerpt_hash`。这样 Observation 可以独立验证和重放“同编号 fallback”与“不同编号候选”，但不保存自由文本推理。

不保存：

- 模型隐藏思维链。
- Planner 自由文本推理。
- 不必要的完整 chunk 正文副本。
- API key、access token 或内部鉴权信息。token 用量是可观测统计，不属于鉴权信息。

持久化策略：

- 查询入口生成 `trace_id` 并创建 running Trace。
- 每次 Planner/Observation 完成后追加结构化 step，避免中途失败时整条 Trace 丢失。
- 正常终止写 completed；未处理异常写 failed 和 `error_code`。
- repository 使用懒加载，不能破坏 graph import/compile 测试。
- Trace 写入失败记录日志，但不应让本来可回答的查询失败；API 仍返回当前 `trace_id`，并在日志中明确持久化失败。

索引建议：

```text
unique(trace_id)
(owner_user_id, started_at)
(session_id, started_at)
(policy_version, retrieval_config_version, started_at)
(planner_type, model_id, policy_version, started_at)
```

### 11. 输出结构化 citations，并保持本地/Web 来源差异

目标：答案展示的引用不是 LLM 自己编写的文本，而是从最终 reranked evidence 确定性生成。

本地引用：

```text
document_id = candidate.document_id
chunk_id = candidate.chunk_id
title = candidate.title or source_title
source = candidate.source_title
score = candidate.rerank_score
source_type = "local"
```

Web 引用：

```text
document_id = null
chunk_id = null
title = web title
source = web url
score = candidate.rerank_score
source_type = "web"
```

要求：

- citations 按最终证据顺序生成并去重。
- 本地优先使用 `document_id + chunk_id` 去重，Web 使用规范化 URL 去重。
- citation 只来自进入答案上下文的最终候选，不能把所有召回结果都当成引用。
- `answer_service` 继续负责生成答案文本和图片 URL，但不能丢失或改写 citations。
- Prompt 明确要求回答只能基于提供证据；引用结构本身由代码生成，不要求 LLM 输出可解析 JSON。

API/SSE 调整：

- 非流式 `QueryNotStreamResponse` 增加 `trace_id`、`citations`、`terminal_reason_code`。
- 流式 FINAL 事件增加相同字段。
- 后台轮询最终结果也透传相同字段。
- 聊天页在答案下方显示轻量引用列表；本地引用显示标题与 document/chunk 标识，Web 引用显示可点击 URL。

### 12. 版本化 Planner 与检索配置

目标：Trace 和评测结果能关联到一份明确配置，而不是只记录零散常量。

建议在 `app/rag/query/config.py` 中集中定义：

```text
POLICY_VERSION = "rule-v1"
RETRIEVAL_CONFIG_VERSION = "retrieval-v1"
PLANNER_MAX_STEPS
RETRIEVAL_MODE
RETRIEVAL_PER_CHANNEL_TOPK
RRF_K
RERANK_EVIDENCE_THRESHOLD
REALTIME_PATTERNS_VERSION
WEB_FALLBACK_ENABLED
```

Trace 保存配置快照，至少包括：

```text
retrieval_mode
per_channel_topk
fusion_topk
rerank_min_topk
rerank_max_topk
rrf_k
evidence_threshold
web_fallback_enabled
```

环境变量可选择运行模式，但版本号和实际快照必须一起记录，不能只记一个模糊的 `v1`。

## BM25 专项评测与 schema 决策门

### 评测集

阶段 5 建立一个小而真实、只用于检索 schema 选择的设备运维专项集，建议 24～40 条，至少覆盖：

| 类型 | 示例 | 重点 |
|---|---|---|
| 报警码 | `E021/E020 是什么故障` | 精确编号与多编号 |
| 设备型号 | `HAK180/HAK-180/HAK 180` | 变体归一化 |
| SOP 编号/类型 | `SOP-xxx 的停机步骤` | 词法命中与流程语义 |
| 零件编号 | `某备件编号如何更换` | 精确字面召回 |
| 中文部件名 | `急停按钮复位后仍报警` | 中文分词与语义 |
| 纯语义问题 | `设备运行时温度不稳定怎么排查` | dense 语义召回 |

每条至少标注：

```text
query_id
query
dataset_id
owner_user_id
expected_subject_id
expected_chunk_ids
expected_identifiers
expected_identifier_resolution_status
expected_suggested_identifiers
should_answer
should_use_web
```

评测集分为开发集和小型 held-out 集。Analyzer、阈值和组合参数只在开发集调整，held-out 只用于最后确认，避免根据测试结果反复调参。

### 公平对比条件

三组实验必须保持：

```text
相同 collection 数据快照
相同 chunk
相同 query
相同 user/dataset/visibility/enabled 过滤
相同 per-channel top-k
相同 RRF k 和 fusion top-k
相同 reranker 与 rerank top-k
相同 expected chunk 标注
```

对比：

```text
dense + learned sparse
dense + BM25
dense + learned sparse + BM25
```

指标：

```text
recall@k
MRR
nDCG
引用命中率
平均/P95 召回延迟
索引大小
单文档入库耗时
```

### 决策规则

在运行 held-out 之前冻结判断规则，避免看到结果后主观选择：

- 某组合在至少两个核心质量指标上有稳定、实质提升，且其他核心指标没有明显回退，才认为有保留价值。
- 如果质量差距很小，选择字段更少、入库更快、运行依赖更少的组合。
- 精确编号类 query 的 recall 不能为了平均指标被牺牲；必须单独查看报警码、型号、SOP/零件编号子集。
- 三路只有在相对最佳双路仍有稳定增益时才保留，不能因为“技术更多”保留。
- 结论、原始结果、最终 schema 和删除项写入阶段 5 评测报告。

最终只能形成以下一种明确结论：

```text
A. 保留 dense + learned sparse，删除 BM25
B. 保留 dense + BM25，删除 learned sparse
C. 保留 dense + learned sparse + BM25
```

## 接口与数据契约变化

### QueryRequestParam

```text
query: str
session_id: str
is_stream: bool
dataset_ids: list[str]
```

HTTP header：

```text
X-User-Id: str
```

### QueryGraphState

新增核心字段：

```text
owner_user_id
tenant_id
dataset_ids
trace_id
planner_step
planner_action_history
current_planner_decision
retrieval_observation
policy_version
planner_type
planner_runtime_metadata
retrieval_config_version
retrieval_mode
query_identifiers
subject_resolution_status
subject_candidates
clarification_question
citations
terminal_reason_code
answer_runtime_metadata
```

### 查询响应

非流式和流式最终包统一新增：

```text
trace_id
citations
terminal_reason_code
```

### Mongo

新增 collection：

```text
retrieval_traces
```

阶段 5 不新增对外 Trace 管理 API。

### Milvus CHUNKS_COLLECTION

阶段 5A 临时字段：

```text
sparse_vector -> learned_sparse_vector
lexical_text
bm25_sparse_vector
BM25 Function(lexical_text -> bm25_sparse_vector)
```

阶段 5B 根据评测结论删除未保留字段/Function，不保留无用兼容别名。

## 预计文件改动

### 新增

```text
app/rag/query/contracts.py
app/rag/query/planner.py
app/rag/query/query_identifier_service.py
app/rag/query/trace_service.py
app/infra/persistence/retrieval_trace_repository.py
app/process/query/agent/nodes/node_query_planner.py
app/process/query/agent/nodes/node_retrieval_observation.py
app/process/query/agent/nodes/node_terminal_response.py
app/process/query/agent/nodes/node_trace_finalize.py
tests/test_query_planner_stage5.py
tests/test_query_access_filter_stage5.py
tests/test_query_identifier_stage5.py
tests/test_query_trace_stage5.py
tests/test_query_api_stage5.py
tests/test_retrieval_modes_stage5.py
tests/test_bm25_schema_stage5.py
tests/test_query_graph_stage5.py
```

评测数据和脚本的最终位置在实现前统一选定，建议：

```text
evaluation/stage5/retrieval_cases.jsonl
evaluation/stage5/run_retrieval_eval.py
evaluation/stage5/阶段5检索评测报告.md
```

### 重点修改

```text
app/process/query/agent/state.py
app/process/query/agent/main_graph.py
app/process/query/agent/nodes/node_subject_name_confirm.py
app/process/query/agent/nodes/node_search_embedding.py
app/process/query/agent/nodes/node_search_embedding_hyde.py
app/process/query/agent/nodes/node_web_search_mcp.py
app/process/query/agent/nodes/node_rrf.py
app/process/query/agent/nodes/node_rerank.py
app/process/query/agent/nodes/node_answer_output.py
app/rag/query/subject_name_confirm_service.py
app/rag/query/chunk_retrieval_utils.py
app/rag/query/embedding_search_service.py
app/rag/query/hyde_search_service.py
app/rag/query/web_search_service.py
app/rag/query/rrf_service.py
app/rag/query/rerank_service.py
app/rag/query/answer_service.py
app/rag/query/config.py
app/rag/import_/index_service.py
app/infra/vectorstore/milvus_gateway.py
app/shared/clients/milvus_utils.py
app/api/schema/query_schema.py
app/api/http/query_server.py
app/resources/http/chat.html
docs/数据字典.md
重构方案.md
```

具体实现时如果职责已由现有文件清晰承载，不为凑目录机械拆文件；但 Planner 契约、Trace repository 和设备标识提取应保持独立，避免重新把所有逻辑堆回节点。

## 分批实施顺序

阶段 5 必须小批次推进，每一批通过专项测试后再进入下一批。

### 第一部分：查询身份与权限过滤基线

- 查询 API/聊天页增加 `X-User-Id`。
- state 增加 owner/tenant/dataset。
- 统一基础过滤表达式。
- 普通检索和 HyDE 都使用相同权限过滤。
- 补 user_a/user_b/public/shared/enabled/dataset 测试。

完成标准：即使 Planner 尚未接入，现有本地检索也不能跨用户召回私有 chunk。

### 第二部分：契约与 RuleBasedPlanner 纯函数

- 定义 Action、Decision、EvidenceSummary、Observation、Citation、PlannerContext。
- 实现 reason code、合法转移和最大步骤校验。
- 用固定 Observation 序列完成纯函数测试，覆盖高质量证据、低质量证据和检索后歧义。

完成标准：不接 Milvus、不调用 LLM 也能完整重放规则 Action Trace。

### 第三部分：检索候选结构与设备标识

- 修复 rerank 前后元数据丢失。
- 实现标识提取、归一化、结构化过滤和零命中降级。
- 改造 RRF 为空时安全返回。

完成标准：最终候选能稳定携带引用字段，编号类 query 有确定性行为。

### 第四部分：BM25 实验 schema 与三种召回模式

- 先验证 Server/Analyzer 能力。
- 重建临时三路 schema。
- 重新导入固定测试文档。
- 实现 2/3 路请求和 RRF。
- 运行专项评测并冻结结论。

完成标准：同一批 query 能输出三组可比较结果和资源成本。

### 第五部分：Planner 驱动 LangGraph

- 改主体确认输出。
- 新增 Planner、Observation、terminal 节点。
- Web 改 fallback。
- 接入异常 Observation 和安全收口。
- 更新 graph compile/路由测试。

完成标准：真实图可以执行多轮“决策 -> Action -> Observation”，且没有循环。

### 第六部分：Trace、引用与 API/前端交付

- 新增 retrieval trace repository。
- 逐步写入 Action/Observation/结果摘要。
- 写入 Planner/答案模型的实现类型、版本、token、耗时和分项成本，并在持久化前移除运行时证据正文片段。
- API/SSE 返回 trace_id/citations。
- 聊天页轻量展示引用。

完成标准：一次成功、一次追问、一次拒答、一次失败查询都能留下正确终态 Trace。

### 第七部分：最终 schema 收口与全量回归

- 根据评测结论删除未保留通道。
- 删除并重建最终 chunk collection。
- 重新导入公共 demo 和用户私有测试文档。
- 跑专项测试、全量测试和人工验收。
- 同步数据字典、重构方案和评测报告。

完成标准：代码、最终 schema、配置、文档和评测结论完全一致，不遗留临时实验分支。

## 测试计划

### 1. Planner 单元测试

- 主体歧义 -> `ask_clarification`。
- 主体未找到且非实时 -> `refuse`。
- 明显实时问题在初始决策时 -> `web_search`，不预先并发执行 local/HyDE。
- 已确认主体 -> `local_search`。
- 编号只找到不同候选 -> `ask_clarification(IDENTIFIER_CONFIRMATION_REQUIRED)`，不得 answer。
- 编号和候选都未找到 -> `ask_clarification(IDENTIFIER_NOT_FOUND)`。
- local 高分 -> `answer`。
- local 低分 -> `hyde_search`。
- local 空 -> `web_search`。
- local/HyDE 证据存在可解决歧义 -> `ask_clarification`。
- HyDE 高分 -> `answer`。
- HyDE 仍低分 -> `web_search`。
- Web 有结果 -> `answer`。
- Web 空/失败 -> `refuse`。
- 重复 Action、非法转移、超过最大步骤 -> 安全拒答。
- 固定 Observation 重放两次，Decision 序列完全相同。
- 同一 query 输入高质量 local Observation 时走 `local -> answer`，输入低质量 local Observation 时走 `local -> hyde -> answer`。
- 使用实现 `QueryPlanner` 协议的 Fake Planner 替换 `RuleBasedPlanner`，LangGraph 检索、HyDE、Web 和答案节点无需修改。

### 2. 权限过滤测试

- user_a 能召回自己的 private chunk。
- user_b 不能召回 user_a 的 private chunk。
- public/shared 在当前轻量语义下可召回。
- 其他 tenant 的 shared 不可召回。
- `enabled=false` 永不召回。
- 不在 `dataset_ids` 中的 chunk 不召回。
- 恶意引号、换行等值不会破坏 filter 表达式。
- 所有 dense/learned sparse/BM25/HyDE 请求 expr 完全一致。

### 3. 标识与召回模式测试

- 型号、报警码、SOP/零件编号正例、变体和反例。
- 精确过滤有命中时优先返回。
- 精确过滤零命中时执行第二段降级。
- 用户输入 `E020`、只召回 `E021` 时只生成建议候选和追问，Citation 为空且不能 answer。
- metadata 漏填但正文仍命中 `E020` 时标记 `fallback_exact_match`，可以继续进入答案证据。
- `E020` 与 `E021` 都存在时不能自动纠正；多个候选、跨设备候选和无权限候选不能静默选择。
- 三种 retrieval mode 生成正确请求。
- 单个本地 Action 的第一层 RRF 支持两路、三路和部分空通道，且不直接相加 dense/learned sparse/BM25 原始分数。
- 跨 Action 第二层 RRF 支持仅 original、original + HyDE、original + Web、original + HyDE + Web、仅 Web。
- 新 Action 完成后从每个 Action 的原始排名一次性重算外层 RRF；交换 Action 列表到达顺序结果不变，不能把上一轮融合结果再次作为 RRF 输入。
- 本地去重键为 `chunk_id`，Web 去重键为规范化 URL；两类候选都有稳定次级排序。
- Web 执行后进入累计候选和统一 rerank，未执行时不创建 Web 伪列表。

### 4. Schema/入库测试

- `lexical_text` 字段启用 Analyzer。
- BM25 Function input/output 字段正确。
- 应用插入数据不携带 `bm25_sparse_vector`。
- lexical_text 包含正文、标题和结构化标识。
- learned sparse 改名后导入/查询引用一致。
- 最终 schema 不包含评测淘汰字段。

### 5. Trace 与引用测试

- 每次查询都有唯一 trace_id。
- 每一步保存 input Observation、Decision 和 output Observation。
- Trace 不包含模型思维链或完整私密 chunk 正文副本。
- Trace 持久化前会移除 Observation 中的 `content_excerpt`，保留证据 ID 和摘要 hash。
- Trace 可区分 Planner 和答案模型的类型、provider、模型/提示词版本、token 用量、耗时和估算成本。
- 规则 Planner 的模型字段为 `null`，token 用量和估算成本为 `0`。
- 正常 answer、追问、拒答、Action 失败、图异常都有正确 Trace 终态。
- citation 只来自最终证据。
- 本地 citation 有 document/chunk ID，Web citation 使用 URL 且本地 ID 为空。
- rerank 后元数据不丢失。

### 6. API/SSE/前端测试

- 缺少 `X-User-Id` 返回 400。
- 同步响应包含 trace_id/citations/reason code。
- 流式 FINAL 包包含相同字段。
- 聊天页查询请求携带稳定 user ID。
- 本地引用和 Web URL 都可正确展示。
- 旧的答案文本和图片展示不回归。

### 7. 图与回归测试

- 新查询图节点和条件边编译成功。
- import graph 不受查询重构影响。
- 查询图 import/compile 不要求 Mongo、Milvus、模型服务在线。
- 阻止 Web 默认并发的图结构测试。
- targeted tests 通过后运行：

```bash
uv run python -m pytest
```

## 数据重建与验收数据准备

阶段 5 允许删除测试 collection 重建，不做迁移：

```text
确认当前没有生产数据
-> 导出/记录阶段 5 专项评测标注
-> 删除受影响 CHUNKS_COLLECTION
-> 用阶段 5A 三路临时 schema 重建
-> 重新导入相同测试文档
-> 运行三组对比评测
-> 冻结最终组合
-> 删除临时 collection
-> 用阶段 5B 最终 schema 重建
-> 再次导入并运行最终验收
```

验收数据至少包含：

- 一组 `visibility=public` 的公共设备运维 demo 文档。
- user_a 的 private 文档。
- user_b 的 private 文档。
- 一条 shared 测试文档。
- 一条 `enabled=false` 测试 chunk（测试夹具可直接构造；人工启停接口仍留阶段 6）。
- 覆盖多个 `index_version` 的可追踪 chunk。

公共 demo 文档的 visibility 设置通过受控测试/初始化脚本完成；阶段 5 不因此提前开放通用 visibility 管理 API。

## 验收标准

### 功能验收

- 只用本地知识库可以稳定回答有充分证据的问题。
- user_a 能召回公共/shared 和自己的 private 文档，不能召回 user_b private 文档。
- user_b 同理，且相同 query 的权限过滤结果正确。
- `enabled=false` chunk 不会被任何本地通道召回。
- Web Search 不再默认并发；只有实时性、空召回、低质量召回或受控失败时触发。
- 主体歧义时追问，证据不足时拒答，不为了完成流程强行生成答案。
- 答案返回并展示结构化引用来源。

### Planner/Trace 验收

- 六个 Action 语义清楚且都由结构化 `PlannerDecision` 驱动。
- Action、Observation、Trace 都有 schema 校验。
- `PlannerContext` 明确包含问题、主体结果、最新 Observation、Action history、步数和合法 Action，Planner 不直接依赖 LangGraph 全量 state。
- 固定 Observation 和配置下，RuleBasedPlanner 能稳定重放相同 Action/`reason_code` 序列。
- 同一问题在不同 Observation 下可稳定生成 `local -> answer`、`local -> hyde -> answer` 等不同的合法轨迹；检索后发现证据歧义时可以追问。
- 超过最大步骤、重复 Action、非法 Action 或 Planner 异常时安全结束。
- Trace 能展示原始/改写问题、标准主题、每步决策、各通道命中、rerank、最终引用、耗时、错误、策略/模型/提示词版本、token 用量和分项成本。
- Trace 不记录模型私有思维链。

### 检索与 schema 验收

- 型号、报警码、SOP/零件编号变体能被归一化并参与结构化/词法检索。
- dense + learned sparse、dense + BM25、三路融合在相同条件下完成对比。
- 评测报告包含 recall@k、MRR、nDCG、引用命中率、延迟、索引大小和入库耗时。
- 最终 schema 已按评测结论删除未保留字段/Function/代码。
- Milvus collection 已用最终 schema 重建并重新导入，不存在历史兼容分支。

### 工程验收

- 查询图和导入图 compile 测试通过。
- 阶段 5 targeted tests 通过。
- 全量 `pytest` 通过；若有环境型失败，必须区分改动失败与外部依赖不可用。
- `docs/数据字典.md`、`重构方案.md`、阶段 5 评测报告与最终代码/schema 一致。
- 当前实现和阶段 6/7/8/9 计划边界在文档中标记准确。

## 风险与控制

### 风险 1：权限表达式括号错误导致越权

控制：过滤表达式只由一个 builder 生成；对完整字符串和真实 fake search expr 做专项测试，所有本地通道禁止自己拼权限条件。

### 风险 2：BM25 Analyzer 破坏设备编号

控制：先运行 Analyzer token 验证；lexical_text 同时写入 canonical 和常见变体；精确字段仍保留结构化过滤，不让 BM25 成为唯一编号能力。

### 风险 3：RRF 后分数不能直接作为证据阈值

控制：Planner 的证据阈值使用 BGE reranker 的归一化分数，不使用 RRF 分数；RRF 只负责候选排序融合。

### 风险 4：Graph 循环或重复调用昂贵工具

控制：合法转移表、已执行 Action 集合、最大步骤和单 Action 一次限制同时生效。

### 风险 5：Trace 写入影响查询可用性

控制：repository 懒加载；Trace 写失败记录日志但不改变权限和答案结果；图异常仍由入口尝试标记 failed。

### 风险 6：阶段 5 临时三路 schema 变成永久负担

控制：把“评测决策 + 删除失败通道 + 最终重建”列为阶段完成条件，没有完成 schema 收口就不能标记阶段 5 完成。

### 风险 7：shared 被误认为完整权限

控制：代码注释、数据字典和 Trace 明确 `shared` 是当前 tenant 内轻量语义；阶段 7 成员模型落地后必须替换。

## 明确假设

- 当前 Mongo/Milvus 仍是少量测试数据，可以删除受影响 collection 后重建。
- 实施 BM25 前会确认实际 Milvus Server 支持 BM25 Function、Analyzer 和三向量字段，不只依据 Python 客户端版本。
- 当前默认 dataset 为 `dataset_default_equipment_ops`，但查询契约使用 list，为后续多 dataset 预留。
- 当前 tenant 为 `tenant_default`，本阶段不建设真实多租户管理。
- 公共 demo 数据会以 `visibility=public` 重新导入；普通用户上传仍默认 private。
- 阶段 5 只保证查询召回权限；聊天历史的完整用户隔离仍按根方案留在阶段 7。
- Evidence threshold 必须通过专项开发集校准，不能在实施前凭经验宣称某个固定分数正确。
- 阶段 5 最终交付必须包含明确的检索组合结论；在没有评测结果前，方案不预判 BM25 或 learned sparse 谁胜出。

## 阶段完成定义

只有同时满足以下条件，阶段 5 才能标记为完成：

1. 查询身份和权限过滤已经在所有本地通道生效。
2. 通用 QueryPlanner、PlannerContext、Action、Observation、Trace 契约和 RuleBasedPlanner 实现已经落地并通过测试。
3. LangGraph 已改为 Planner 驱动的串行 Action 循环，Web 不再默认并发。
4. citations 已贯穿 rerank、answer、API/SSE 和聊天页，Trace 已区分 Planner/答案模型的版本、token、耗时和成本。
5. 三种召回组合已在固定专项集上完成公平对比。
6. 最终 Milvus schema 已根据评测结论收口并重建。
7. 公共/private/shared/enabled/dataset 隔离验收通过。
8. targeted tests 和全量回归通过。
9. 数据字典、根重构方案和评测报告已同步，且没有把阶段 6/7/8/9 的计划写成当前实现。
