# 阶段 8.5：阶段 9 前置数据处理与 Reward v1.1 方案

## 背景与目标

阶段 8 已完成第一版评测闭环：`PlannerEvalCase` 样本契约、`EnvironmentSnapshot` 环境快照、离线 `Environment`、`reward-v1`、baseline runner、SFT 数据导出和阶段 8 评测报告已经能串起来。

进入阶段 9 前还需要处理两件事：

1. 制造业公开数据处理：把公开设备手册、维护 SOP、报警码说明、零件说明和部分公开制造业数据转成可审核、可复现的 Planner 训练候选池。
2. Reward 优化：把当前 Reward v1 的六路分项权重升级为 Reward v1.1，让 Planner 训练更重视检索路线、fallback 和回答时机，而不是让 chunk 排名这类间接指标主导训练。

本阶段目标不是训练模型，而是在阶段 9 的 SFT 和 GRPO 前，把数据来源、数据质量、证据强度和 Reward 版本冻结清楚。阶段 9 只能使用本阶段审核通过或 Reward 过滤通过的数据，不能把来源不清或未审核的新数据直接混进训练。

## 历史边界

- 阶段 8 报告使用 `reward-v1`，它只作为历史评测结果保留。
- 阶段 8.5 起升级为 `reward-v1.1`，新增数据筛选、评测报告和训练导出都以 `reward-v1.1` 为准。
- 阶段 9 只使用 `reward-v1.1` 或后续版本。
- 不再维护 `reward-v1` 的新增评测和训练导出。

## 关键术语边界

- `Planner`：规划器，负责根据当前问题和 Observation 选择下一个 Action。阶段 9 训练的是 Planner Policy，不训练最终答案模型。
- `Action`：Planner 可输出的固定动作，例如 `local_search`、`hyde_search`、`web_search`、`answer`、`ask_clarification`、`refuse`。
- `Observation`：Action 执行后的可验证事实，例如召回数量、rerank 分数、命中的 chunk、错误码和耗时。
- `Trace`：一次查询的结构化轨迹记录，用来复盘 Planner 每一步 Action、Observation、Citation、成本和终态，不保存模型私有思维链。
- `Reward`：奖励或评分信号。它把一条 Trace 按格式、检索、引用、答案、行为和成本拆分评分，阶段 9 的 GRPO 会直接使用总分。
- `SFT`：监督微调，用审核通过的样本教模型输出合法 `PlannerDecision` 和基本路由。
- `GRPO`：强化学习优化方法，本项目只用于优化多步 Planner 轨迹，不放进线上查询路径。
- `expected_chunks`：人工期望命中的证据 chunk 列表，必须包含 `document_id + chunk_id + index_version`，用于判断检索和引用是否命中同一版证据。
- `split`：数据用途拆分，包含 `train/dev/test/demo_regression`。`test` 是 held-out test，即隔离测试集，不能参与调参、模型选择、Reward 权重选择或训练轨迹挑选。
- `leakage_group_id`：泄漏隔离组。同一来源文档、同一报警码、同一 SOP 或同一问题改写必须放在同一个 split，防止训练集和测试集互相泄漏。

## 当前事实

- 根目录 `重构方案.md` 已明确阶段 8.5 是阶段 8 和阶段 9 之间的独立数据工程，不阻塞阶段 8 验收，也不替代阶段 8 的评测环境或 baseline。
- `重构方案/阶段8.md` 中的“任务 8.5：实现 Reward v1”已经完成，它不是本文件说的独立阶段 8.5。后续命名时要区分“阶段 8 内部任务 8.5”和“阶段 9 前置阶段 8.5”。
- `app/rag/evaluation/case_schema.py` 已有 `ExpectedChunk.relevance`，取值为 `required/supporting/acceptable`，但阶段 8.5 暂不把证据强度和 chunk 排名扩展成新的训练主指标。
- `app/rag/evaluation/reward.py` 当前 `REWARD_VERSION = "reward-v1"`，六路权重为 `format=0.15`、`retrieval=0.25`、`citation=0.15`、`answer=0.20`、`behavior=0.15`、`cost=0.10`。其中 `retrieval + citation = 0.40` 偏高，`behavior + cost = 0.25` 偏低，不适合作为阶段 9 Planner 训练的默认权重。
- `evaluation/stage8/reports/阶段8评测报告.md` 已记录当前阶段 8 结论：可以进入阶段 9 的基本条件，但当前 `snapshot_expected_chunks` provider 只证明离线管线可用，不代表真实公开数据质量已经足够训练。

## 总体路线

```text
历史边界确认
    -> 公开来源采集和许可证记录
    -> 文档清洗、切 chunk 和来源映射
    -> 表格/时序/图像数据转故障场景卡片
    -> 自动生成候选问题、答案要点和 expected_chunks
    -> schema 校验、重复校验、split 泄漏校验
    -> 人工审核关键样本
    -> Reward v1.1 按 Planner 可控性重标定权重
    -> 拆分 train/dev/test，冻结数据版本和环境快照
    -> 阶段 9 SFT/GRPO 只读取通过边界的数据
```

阶段 8.5 分两条线并行推进，但最终必须在同一份数据版本里汇合：

- 数据线：负责来源、清洗、候选生成、审核、拆分和冻结。
- Reward 线：负责 Reward v1.1 的六路权重重标定、单元测试、报告生成和训练导出边界更新。

## 范围与非目标

本阶段要做：

- 新增制造业公开数据处理目录、来源清单、许可证记录、处理脚本入口和数据质量报告。
- 把公开文档转成可进入 RAG 的文档和 chunk，并保留来源文档、段落、页码或章节映射。
- 把原始传感器表格、时序、音频或图像类公开数据先转成可解释的故障场景卡片，再决定是否生成 RAG 问答样本。
- 自动生成候选 `PlannerEvalCase`，但只让通过 schema 校验、泄漏校验、人工审核或 Reward 过滤的样本进入训练候选。
- 升级 Reward v1.1，使 `behavior + cost` 这类检索路线分成为 Planner 训练的主信号，`retrieval + citation` 只保留为低权重终局质量约束。
- 用 `reward-v1.1` 重跑阶段 8.5 dev/test 评测，确认新增数据、Reward 版本和训练导出边界一致。

本阶段不做：

- 不执行 SFT 或 GRPO 训练。
- 不把新增公开数据直接写进 `sft_planner_train.jsonl`。
- 不把 Demo 回归集扩大成训练集。
- 不把 held-out test 用于生成 Prompt、选择模型、调 Reward 权重或挑选训练轨迹。
- 不把原始传感器表格、音频、时序信号或图片直接塞进向量库当 RAG 文档。
- 不把 LLM Judge 作为主 Reward 替代规则指标。
- 不修改线上查询路径，不启用 Model Planner。

## 推荐目录结构

新增阶段 8.5 目录，避免污染阶段 8 已有结果：

```text
evaluation/stage8_5/
  sources/
    source_manifest.jsonl
    license_manifest.jsonl
  raw/
    .gitkeep
  processed/
    public_documents_manifest.jsonl
    fault_scenario_cards.jsonl
    chunk_source_map.jsonl
  candidates/
    planner_case_candidates.jsonl
    review_queue.jsonl
    rejected_cases.jsonl
    approved_cases.jsonl
    split_manifest.json
  results/
    reward_v1_1_baseline_dev.json
    reward_v1_1_baseline_train.json
    data_quality_report.json
  reports/
    阶段8.5数据处理报告.md
```

可复用和可测试的业务逻辑继续放在：

```text
app/rag/evaluation/
  metrics.py
  reward.py
  case_schema.py
```

如果某段逻辑只服务一次性数据处理和报告生成，放在 `evaluation/stage8_5/` 下，不进入线上 `app/`。

## 数据处理方案

### 1. 来源分级

优先级 1：最适合直接做 RAG 问答的数据。

- 设备操作手册。
- 维护 SOP。
- 报警码说明。
- 安全注意事项。
- 零件、部件、易损件说明。
- 维修排查流程和点检表。

优先级 2：可以作为补充，但必须先结构化的数据。

- 公开制造业质量检测数据。
- 故障诊断数据。
- 预测性维护数据。
- 缺陷图像或传感器表格。

这类数据不能直接作为 RAG 文档，需要先转成“故障场景卡片”，例如：

```text
设备/部件
    -> 故障现象
    -> 可能原因
    -> 诊断步骤
    -> 处理建议
    -> 安全注意事项
    -> 证据来源和字段解释
```

优先级 3：社区或二次整理数据。

- 只能作为候选来源。
- 必须检查许可证、来源可信度、字段含义、是否允许再分发、是否允许训练使用。
- 许可证不明确时，只进入内部调研清单，不进入训练候选池。

### 2. 来源清单契约

`source_manifest.jsonl` 每行记录一个来源：

| 字段 | 含义 |
|---|---|
| `source_id` | 来源稳定 ID，全局唯一。 |
| `source_type` | 来源类型，例如 `manual/sop/alarm_code/table/image/community`。 |
| `title` | 来源标题。 |
| `publisher` | 发布方或维护方。 |
| `url_or_path` | 原始 URL 或本地路径。 |
| `collected_at` | 采集时间。 |
| `source_hash` | 原文件 hash，用于发现内容漂移。 |
| `license_name` | 许可证名称。 |
| `license_url` | 许可证说明地址，可为空但必须记录原因。 |
| `redistribution_allowed` | 是否允许再分发。 |
| `training_allowed` | 是否允许用于训练。 |
| `commercial_use_allowed` | 是否允许商业使用。 |
| `approval_status` | `approved/pending/rejected`。 |
| `reject_reason` | 拒绝或暂缓原因。 |

硬规则：

- `approval_status != approved` 的来源不能进入 `approved_cases.jsonl`。
- `training_allowed != true` 的来源不能进入 SFT/GRPO 训练候选。
- 来源、许可证或字段含义说不清的数据，宁可丢弃，不进入训练。

### 3. 故障场景卡片契约

`fault_scenario_cards.jsonl` 每行表示一个可解释的故障或维护场景：

| 字段 | 含义 |
|---|---|
| `card_id` | 场景卡片 ID。 |
| `source_id` | 关联来源。 |
| `source_document_id` | 转成 RAG 文档后的 document ID，可为空。 |
| `source_section` | 来源章节、页码、表格名或字段名。 |
| `equipment_model` | 设备型号。 |
| `component_name` | 部件或零件名称。 |
| `alarm_code` | 报警码，可为空。 |
| `symptom` | 故障现象。 |
| `possible_causes` | 可能原因。 |
| `diagnostic_steps` | 排查步骤。 |
| `maintenance_actions` | 处理建议。 |
| `safety_notes` | 安全注意事项。 |
| `evidence_text` | 可追溯的原文证据或字段解释。 |
| `evidence_chunk_ids` | 对应 chunk 身份，必须带版本。 |
| `quality_flags` | 质量标记，例如 `missing_license/weak_evidence/ambiguous_identifier`。 |

场景卡片只解决“原始数据如何变得可解释”，不等于训练样本。只有能稳定映射到 `expected_chunks` 和答案要点的卡片，才可以生成 Planner case。

### 4. 候选样本生成

候选生成优先从每张故障场景卡片生成以下问题类型：

- 设备型号 + 报警码含义。
- 故障现象原因排查。
- 维护 SOP 步骤。
- 安全注意事项。
- 零件或部件定位。
- 主体歧义追问。
- 证据不足拒答。
- 明显实时问题的 Web 行为边界。

生成后的 `planner_case_candidates.jsonl` 必须尽量复用阶段 8 的 `PlannerEvalCase` 字段，不另起一套训练格式。新增来源追踪字段可以放在 `notes` 或阶段 8.5 的外部 manifest 中，避免破坏已落地 schema。

候选样本的最低要求：

- `case_id` 稳定且唯一。
- `leakage_group_id` 至少按 `source_id + source_section + equipment_model + alarm_code/SOP` 生成。
- 回答型样本必须有 `expected_chunks` 和 `expected_answer_points`。
- `expected_chunks.relevance` 必须明确标成 `required/supporting/acceptable`。
- `acceptable_action_paths` 必须反映业务预期，不能为了让轨迹拿高分而放宽到所有路径。
- `label_source` 对自动生成样本先标 `synthetic`，人工复核后才能成为高可信样本。
- `human_review_status` 默认 `pending`，不能默认写成 `reviewed`。

### 5. 质量门禁

新增数据必须经过九道门禁：

| 门禁 | 目的 | 不通过处理 |
|---|---|---|
| 来源门禁 | 检查来源和许可证。 | 写入 `rejected_cases.jsonl` 或保留在 pending 来源。 |
| 解析门禁 | 检查文本提取、表格解释和乱码。 | 不生成 case，只保留处理日志。 |
| chunk 门禁 | 检查空 chunk、重复 chunk、页眉页脚和弱证据。 | 禁用或不参与候选生成。 |
| 场景门禁 | 检查故障场景卡片是否可解释。 | 只保留卡片，不生成训练样本。 |
| schema 门禁 | 用 `PlannerEvalCase` 校验字段。 | 直接拒绝。 |
| 泄漏门禁 | 检查 `case_id`、`leakage_group_id` 和来源文档不跨 split。 | 重新分组或拒绝。 |
| 检索门禁 | 用当前 snapshot 检查 expected chunk 是否可召回。 | 进入人工复查，不直接训练。 |
| 人工门禁 | 审核关键样本的答案要点、证据强度和行为路径。 | pending/rejected 不导出训练。 |
| Reward 门禁 | 用 Reward v1.1 过滤低质量轨迹。 | 路线错误、成本异常或终局质量不足的轨迹不进入 SFT/GRPO。 |

## Reward v1.1 方案

### 1. 为什么必须在阶段 9 前升级

当前 Reward v1 是阶段 8 离线评测用的第一版综合分。它可以衡量检索和引用质量，但默认权重不完全符合阶段 9 的 Planner 训练目标：

- `retrieval + citation` 合计权重为 0.40，容易让 chunk 命中和 Citation 这类终局证据指标主导总分。
- `behavior + cost` 合计权重为 0.25，但 Planner 直接控制的恰恰是是否检索、是否 fallback、何时回答、何时追问/拒答以及是否多走无意义步骤。
- 同一个固定检索动作下，chunk 排名主要由检索器、reranker、chunk 质量和标注决定，不是 Planner 直接输出。

因此阶段 8.5 不再把二值检索、证据强度和 chunk 排名扩展成新的训练主指标。Reward v1.1 的重点改为：按 Planner 可控性重新分配六路权重，让阶段 9 的 GRPO 优先学习正确检索路线。

### 2. Planner 可控性原则

Planner 直接控制：

- 输出合法 `PlannerDecision`。
- 选择 `local_search`、`hyde_search`、`web_search`、`answer`、`ask_clarification` 或 `refuse`。
- 根据 Observation 决定是否继续检索、fallback、回答、追问或拒答。
- 控制 Action 步数、是否滥用 HyDE/Web、是否过早回答。

Planner 间接影响：

- 哪些 chunk 被召回。
- 最终 Citation 是否覆盖 expected chunk。
- 答案要点是否完整。

Planner 基本不直接控制：

- 同一路检索器内部的 chunk 排名。
- reranker 对候选 chunk 的排序细节。
- chunk 本身质量和 expected chunk 标注粒度。

所以 Reward v1.1 的训练权重应该遵循：

| 分项 | 训练定位 | 权重方向 |
|---|---|---|
| `format` | 保证结构化输出和 Action 合法性 | 中等偏高 |
| `retrieval` | 终局证据质量约束，不主导 Planner 训练 | 降低 |
| `citation` | 防止无引用、伪造引用或引用错版本 | 降低 |
| `answer` | 约束是否到达正确终态和答案要点 | 中等 |
| `behavior` | 训练该答、该拒、该追问、该 Web 的路线决策 | 最高 |
| `cost` | 约束无意义 HyDE/Web、额外步骤和延迟 | 中高 |

### 3. 建议默认权重

Reward v1.1 第一版权重：

```text
format    = 0.15
retrieval = 0.12
citation  = 0.08
answer    = 0.15
behavior  = 0.35
cost      = 0.15
```

含义：

- `behavior + cost = 0.50`，作为“检索路线分”的主信号。
- `retrieval + citation = 0.20`，保留终局证据质量约束，但不让 chunk 排名主导训练。
- `format + answer = 0.30`，保证输出合法、终态正确、答案要点不被完全忽略。

暂不调整 `score_retrieval` 内部公式，继续使用现有 `recall@k`、MRR、nDCG 和标识命中率。它们先作为评测指标和低权重终局信号保留，不在阶段 8.5.2 新增 `weighted_recall_at_k`、`weighted_ndcg_at_k` 或 required gate。

### 4. 训练前有效性验证

阶段 9 真正训练前，必须验证六路分项在不同 Planner 轨迹之间是否有有效差异。

验证方法：

```text
同一批 dev case
    -> 构造或采样多条 Action path
    -> 计算 format/retrieval/citation/answer/behavior/cost
    -> 观察每个分项在不同路径之间的方差和排序相关性
```

判断规则：

- 如果 `behavior/cost` 能明显区分正确路线和错误路线，保持高权重。
- 如果 `retrieval/citation` 在不同 Planner 轨迹之间几乎不变，继续低权重，只作为报告指标。
- 如果某些 case 中 `hyde_search` 或 `web_search` 的路线选择确实显著改善证据质量，可以保留 `retrieval/citation` 的辅助作用，但不升为主信号。
- 如果 `cost` 过高导致模型倾向不检索就拒答，需要降低 `cost` 或提高 `behavior/answer` 对正确回答的约束。

### 5. 版本与兼容边界

- `REWARD_VERSION` 升级为 `reward-v1.1`，旧的 `reward-v1` 结果只作为阶段 8 历史报告保留。
- 阶段 8.5 的新增评测、数据筛选和训练导出不再生成 `reward-v1` 结果。
- 阶段 8.5.2 只调整六路权重和对应说明，不改检索指标内部公式。
- dev 上可以调六路权重；test 运行前必须冻结 `reward_version` 和权重。
- SFT 导出 manifest 必须记录使用的 `reward_version=reward-v1.1` 或后续版本。

## 实施顺序

### 任务 8.5.1：设计公开数据处理脚本入口

目标：先建立来源、许可证和处理产物边界，不急着大规模采集。

建议脚本：

```text
evaluation/stage8_5/validate_sources.py
evaluation/stage8_5/build_fault_cards.py
evaluation/stage8_5/generate_candidate_cases.py
evaluation/stage8_5/validate_candidate_cases.py
evaluation/stage8_5/split_candidate_cases.py
evaluation/stage8_5/generate_stage85_report.py
```

验收标准：

- 来源清单可校验。
- 许可证不明确的数据不会进入 approved。
- 候选 case 可以复用阶段 8 的 schema 校验。
- 输出候选池、审核池、拒绝池和通过池的数量统计。

当前进度：**任务 8.5.1 已完成第一版脚本入口和文件化门禁骨架。**

- 已新增 `evaluation/stage8_5/stage85_schema.py`，定义 `SourceRecord`、`LicenseRecord`、`FaultScenarioCard`、`EvidenceChunkRef`、`Stage85Issue` 和 `Stage85QualityReport`。
- 已新增六个脚本入口：
  - `validate_sources.py`
  - `build_fault_cards.py`
  - `generate_candidate_cases.py`
  - `validate_candidate_cases.py`
  - `split_candidate_cases.py`
  - `generate_stage85_report.py`
- 已新增 `sources/`、`processed/`、`candidates/`、`results/` 和 `reports/` 初始目录及空清单文件。
- 当前边界是只实现入口、schema 校验、分流和统计，不采集真实公开数据、不导入知识库、不生成训练数据。

### 任务 8.5.2：实现 Reward v1.1 权重重标定

目标：按 Planner 可控性重标定六路权重，阶段 9 不再让 chunk 排名这类间接指标主导 Planner 训练。

改动范围：

- `app/rag/evaluation/reward.py`
  - 升级 `REWARD_VERSION`。
  - `RewardWeights` 默认值改为 `format=0.15`、`retrieval=0.12`、`citation=0.08`、`answer=0.15`、`behavior=0.35`、`cost=0.15`。
  - 字段注释说明 `behavior + cost` 是检索路线分，`retrieval + citation` 是低权重终局质量约束。
  - 暂不改 `score_retrieval` 内部公式。
- `tests/test_stage8_reward.py` 或新增 `tests/test_stage85_reward_v1_1.py`
  - 覆盖 Reward v1.1 默认六路权重。
  - 覆盖不必要 HyDE/Web 通过 behavior/cost 拉开总分。
  - 覆盖 retrieval/citation 权重低于 behavior/cost。
  - 覆盖没有 expected chunk 的拒答/追问样本不被检索分误扣。

验收标准：

- Reward v1.1 单元测试通过。
- Reward 输出可 JSON 序列化。
- 报告能展示六路分项均值和总分。
- dev 验证能说明 `behavior/cost` 对不同 Action path 的区分度高于或不低于 `retrieval/citation`。

### 任务 8.5.3：生成第一批公开数据候选池

目标：先做小规模、可审计、可人工抽查的数据闭环，不追求一次性做成大平台。

建议规模：

- 来源 3-5 个，通过许可证筛选后再处理。
- 生成候选 case 50-150 条。
- 人工审核第一批核心样本 20-40 条。
- 新增 train/dev/test 时优先按来源文档和 `leakage_group_id` 隔离。

验收标准：

- 每条 approved case 能追溯到 `source_id`、文档、chunk 版本和生成方式。
- 回答型样本都有 required expected chunk。
- pending/rejected 样本不会进入 SFT 导出。

### 任务 8.5.4：重跑评测和 SFT 导出候选

目标：确认新 Reward 和新增数据不会让指标漂移失控。

建议命令形态：

```text
uv run python evaluation/stage8/run_planner_eval.py \
  --cases evaluation/stage8_5/candidates/approved_cases.jsonl \
  --snapshot evaluation/stage8/snapshots/environment_snapshot.json \
  --split dev \
  --planner rule \
  --output evaluation/stage8_5/results/reward_v1_1_baseline_dev.json
```

如果新增公开数据已导入知识库，需要生成新的环境快照，不覆盖阶段 8 原快照：

```text
uv run python evaluation/stage8/build_environment_snapshot.py \
  --output evaluation/stage8_5/results/environment_snapshot_stage85.json
```

验收标准：

- dev/test 使用的 snapshot、case、Reward 版本都写入结果。
- `reward_v1_1_baseline_dev.json` 能展示 `reward-v1.1` 的六路分项变化和路线质量差异。
- 新 SFT 候选导出记录 `reward_version=reward-v1.1`。

## 阶段 9 进入条件

满足以下条件后，才建议进入阶段 9：

- 已明确历史边界：阶段 8 报告保留 `reward-v1`，阶段 8.5 和阶段 9 不再新增 `reward-v1` 评测或训练导出。
- Reward v1.1 已落地并通过测试。
- Reward v1.1 已在 dev 上确定六路权重，test 运行前不再改。
- 新增公开数据有来源清单、许可证记录和处理脚本入口。
- approved case 可以通过 `PlannerEvalCase` schema 校验。
- split manifest 能证明 `leakage_group_id` 不跨 train/dev/test。
- SFT 候选只来自 train split，且通过 Reward 阈值或人工复核。
- held-out test、demo regression 不进入训练导出。
- 阶段 8.5 报告能列出候选池、人工审核池、可训练池和 held-out test 池的数量统计。

## 风险与处理

| 风险 | 处理 |
|---|---|
| 许可证不清导致数据不能训练 | 来源门禁前置，`training_allowed=false/pending` 的来源不进入训练。 |
| 自动生成的问题或答案要点幻觉 | 所有回答型样本必须绑定 expected chunk 和答案要点，关键样本人工审核。 |
| 同源问题跨 split 泄漏 | `leakage_group_id` 按来源、章节、设备、报警码或 SOP 生成，并在 split 校验中强制唯一归属。 |
| Reward v1.1 过度惩罚复杂问题 | 只在 dev 上调六路权重，test 前冻结；报告展示分项和扣分原因。 |
| 模型为了 Reward 滥用 HyDE/Web | 保留 `R_cost` 和 forbidden actions，报告展示不必要 HyDE/Web 调用率。 |
| 原始传感器数据不可解释 | 先转故障场景卡片，不能解释清楚来源和字段含义就不生成 RAG 样本。 |

## 最终交付

- `evaluation/stage8_5/` 数据处理目录和脚本入口。
- `source_manifest.jsonl`、`license_manifest.jsonl`、`fault_scenario_cards.jsonl`。
- `planner_case_candidates.jsonl`、`review_queue.jsonl`、`approved_cases.jsonl`、`split_manifest.json`。
- Reward v1.1 的六路权重配置、明细输出和单元测试。
- `reward_v1_1_baseline_dev.json` 和阶段 8.5 数据处理报告。
- 阶段 9 可读取的 SFT/GRPO 候选数据边界说明。

阶段 8.5 完成后的判断标准很简单：新增数据来源清楚、证据能追溯、Planner 可控的检索路线信号能反映到 Reward、训练导出不会污染 held-out test。只有这四点成立，阶段 9 的 SFT 和 GRPO 对比才有工程意义。
